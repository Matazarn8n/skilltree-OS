---
phase: 04-backend
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
model: sonnet-4-6
effort: medium
autonomous: true
files_modified:
  - apps/web/app/api/me/route.ts
  - apps/web/app/api/progress/route.ts
  - apps/web/app/api/install/route.ts
  - apps/web/app/api/tree/route.ts
  - apps/web/app/api/brain/route.ts
  - apps/web/app/api/brain/draft/route.ts
  - apps/web/app/api/onboarding/route.ts
  - apps/web/app/api/access/route.ts
  - apps/web/app/api/stripe/webhook/route.ts
  - apps/web/lib/installs.ts
  - apps/web/lib/progress.ts
  - apps/web/lib/brain.ts
  - apps/web/components/tree/TreeAudit.tsx
  - apps/web/components/tree/MyTree.tsx
  - tools/verify_p6.py

must_haves:
  truths:
    - "Parcours réel : signup → installer un skill → reload → toujours installé, avec une VRAIE ligne dans la table Postgres `installs` (compteur avant/après vérifié), pas du localStorage."
    - "Les routes user /api/me|progress|install|tree|brain|onboarding|access existent, sont DB-backed et scopées à auth.uid() (un user ne touche que ses lignes via RLS)."
    - "Les routes catalogue (/api/catalog, /api/skills/[slug], /api/my-tree) font 0 requête DB : elles n'importent pas le client Supabase et répondent 200 même DB injoignable."
    - "/api/brain/draft renvoie un draft RÉEL via la gateway :8765 (server-side, header X-Internal-Token — JAMAIS x-api-key ni ANTHROPIC_API_KEY ni Bearer OAuth dans l'app) ; preuve = nouvelle ligne proxy_calls dans ~/hermes-data/hermes.db + contenu NON-gabarit (ne contient pas la phrase du stub) ; gated paid (403 si non-payé) ; 429 au-delà du rate-limit par user."
    - "/api/access renvoie {paid} depuis la DB ; PERSONAL_MODE=true force paid=true (Stripe-ready : route webhook stub présente mais NON exercée, D8)."
    - "Le swap des stubs ne réécrit PAS l'UI présentationnelle : install/uninstall/isInstalled/useInstalls, useProgress, useBrain/draftBrain/loadBrainMap/BRAIN_SECTION_KEYS gardent des SIGNATURES identiques ; seuls les CORPS passent de localStorage à fetch /api. Les composants consommateurs via hooks (InstallModal, MostInstalled, lesson/*, brain/*) n'ont AUCUN diff — la SEULE exception composant est TreeAudit.tsx + MyTree.tsx re-pointés de localStorage brut vers useInstalls() (re-couplage explicitement signalé)."
  artifacts:
    - path: "apps/web/app/api/install/route.ts"
      provides: "POST installe un skill (insert installs, auth.uid()) / DELETE désinstalle / GET liste"
      exports: ["POST"]
    - path: "apps/web/app/api/brain/draft/route.ts"
      provides: "POST draft LLM via gateway :8765 (X-Internal-Token), gated paid, rate-limit par user"
      exports: ["POST"]
    - path: "apps/web/app/api/access/route.ts"
      provides: "GET {paid} ; PERSONAL_MODE=true force paid"
      exports: ["GET"]
    - path: "apps/web/lib/installs.ts"
      provides: "Corps swappé vers fetch /api/install ; signatures install/uninstall/isInstalled/useInstalls inchangées"
      contains: "/api/install"
    - path: "tools/verify_p6.py"
      provides: "Preuve Playwright + DB : signup→install→reload persiste (row Postgres), 0-DB catalogue, draft réel gateway (proxy_calls), gating+rate-limit, access PERSONAL_MODE ; exit 1 au 1er FAIL ; écrit orchestration/verify/p6/"
      min_lines: 80
  key_links:
    - from: "apps/web/lib/installs.ts install()"
      to: "POST /api/install → supabase installs insert"
      via: "fetch server route scopé auth.uid()"
      pattern: "fetch\\(.*/api/install"
    - from: "apps/web/app/api/brain/draft/route.ts"
      to: "gateway http://localhost:8765/api/llm/complete"
      via: "X-Internal-Token (HERMES_INTERNAL_TOKEN), body {prompt,model,max_tokens,source}"
      pattern: "api/llm/complete|X-Internal-Token|x-internal-token"
    - from: "apps/web/lib/brain.ts draftBrain()"
      to: "POST /api/brain/draft"
      via: "fetch (remplace le gabarit déterministe local)"
      pattern: "fetch\\(.*/api/brain/draft"
    - from: "apps/web/components/tree/TreeAudit.tsx + MyTree.tsx"
      to: "useInstalls() (source unique)"
      via: "re-couplage : ne lit plus le localStorage brut skilltree.installs.v1"
      pattern: "useInstalls"
---

<objective>
Remplacer les stubs locaux de Phase 3 par un backend réel : les routes user DB-backed (me/progress/install/tree/brain/onboarding/access), le `/api/brain/draft` qui relaie l'OAuth via la gateway HERMES :8765 (aucune clé Anthropic dans l'app, D7), `/api/access` Stripe-ready derrière PERSONAL_MODE (D8), et le swap des 3 corps de hooks (installs/progress/brain) de localStorage vers `fetch /api` — SANS toucher l'UI qui les consomme. Les routes catalogue restent 0-DB.

Purpose : câblage standard une fois le contrat schéma/auth/RLS figé par 04-01. C'est le passage « stub → vrai state user » qui rend la persistance réelle (row Postgres) et branche le seul point LLM du produit sur l'infra perso, isolé derrière une fonction/URL swappable pour la cible multi-tenant.

Output : 8 routes user + webhook Stripe stub, 3 lib swappées, TreeAudit/MyTree re-couplés, et la preuve tools/verify_p6.py dans orchestration/verify/p6/.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Contrat API (§6) + intégration gateway (§8) + décisions D7/D8
@docs/ARCHITECTURE.md

# Fondation posée par 04-01 : clients Supabase (server/service/browser), schéma RLS, auth
@.planning/phases/04-backend/04-01-SUMMARY.md

# Stubs Phase 3 à swapper — corps only, signatures figées
@apps/web/lib/installs.ts
@apps/web/lib/progress.ts
@apps/web/lib/brain.ts

# Consommateurs à NE PAS réécrire (via hooks) + les 2 à re-coupler (localStorage brut)
@apps/web/components/tree/TreeAudit.tsx
@apps/web/components/tree/MyTree.tsx

# Règle OAuth vs clé API : la gateway :8765 relaie le Bearer OAuth ; l'app envoie X-Internal-Token, JAMAIS x-api-key
@/home/nuveo/.claude/CLAUDE.md
</context>

<constraints>
- Gateway (contrat vérifié sur disque) : `POST http://localhost:8765/api/llm/complete`, header `X-Internal-Token: $HERMES_INTERNAL_TOKEN`, body `{ prompt, model: "fast"|"balanced"|"deep"|"auto", max_tokens, source }`, réponse `{ text, model, backend, alias, routing }`. L'app NE contient AUCUNE clé Anthropic (ni x-api-key, ni ANTHROPIC_API_KEY, ni Bearer OAuth) — la gateway détient l'OAuth. HERMES_GATEWAY_URL + HERMES_INTERNAL_TOKEN viennent de apps/web/.env.local (écrit par 04-01).
- Preuve d'appel LLM réel (anti-faux-positif) : la gateway journalise chaque appel dans `~/hermes-data/hermes.db` (HERMES_DB_PATH) table `proxy_calls` (colonne `source`). Envoyer `source:"skilltree-brain-draft"` et prouver via une NOUVELLE ligne proxy_calls après l'appel + un `text` qui NE contient PAS la phrase du stub (« Brouillon à partir de »). Un draft-gabarit local = échec du critère.
- Gating + rate-limit : `/api/brain/draft` exige un user authentifié PAYÉ (403 si non-payé) et applique un rate-limit PAR USER (llm-cron-cost-guard) — au-delà de N drafts / fenêtre → 429. Rate-limit en mémoire process (Map keyée user id) — le schéma est figé par 04-01, ne PAS ajouter de table. Documenter que le store durable est l'upgrade multi-tenant.
- PERSONAL_MODE=true → `/api/access` force `paid:true` (D8) ; `/api/stripe/webhook` existe (Stripe-ready) mais N'EST PAS exercé (pas de dépendance `stripe` npm, pas de webhook live) — handler structurel qui répond 501/no-op quand STRIPE_WEBHOOK_SECRET absent. NE PAS ajouter de dépendance npm (package.json appartient à 04-01).
- Routes catalogue INTOUCHÉES et 0-DB : ne PAS modifier /api/catalog, /api/my-tree, /api/skills/[slug] ; elles n'importent pas le client Supabase. Prouver : (a) grep = 0 import supabase dans ces 3 routes ; (b) runtime — /api/catalog répond 200 même Supabase injoignable.
- Swap stubs = CORPS seulement. Signatures figées EXACTES : `install/uninstall/isInstalled(slug)`, `useInstalls()` (retourne {installed, isInstalled, install, uninstall, count}), `useProgress()` ({isComplete, markComplete, countDone}), `useBrain()` ({read, save, saveAll, all, reset}), `draftBrain(input)`, `loadBrainMap()`, `firstUnfilledIndex()`, `BRAIN_SECTIONS`, `BRAIN_SECTION_KEYS`. Les composants qui consomment via ces hooks (InstallModal, MostInstalled, hub-data, lesson/*, brain/*) doivent avoir 0 diff.
- RE-COUPLAGE SIGNALÉ : TreeAudit.tsx et MyTree.tsx lisaient le contrat installs par CLÉ/EVENT localStorage brut (`skilltree.installs.v1` / `skilltree:installs`) sans importer lib/installs.ts. Quand installs passe à /api, ce contrat localStorage disparaît → CES DEUX composants DOIVENT être re-pointés sur `useInstalls()` (même source unique). Changer UNIQUEMENT la source de données (l'import + l'accès à la liste installée), PAS le JSX/rendu. C'est la seule modification de composant autorisée dans ce plan.
- Anti-faux-positif : la persistance se prouve par une ROW Postgres (compteur avant/après), jamais par un state client. Le draft se prouve par proxy_calls + contenu non-gabarit, jamais par un 200 seul.
- NOTE EXÉCUTEUR : les 78 fiches skills et l'UI Phase 3 sont figées ; ne pas les régénérer. Worktree isolation recommandée si un jour exécuté en parallèle (ici séquentiel, risque faible).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Routes user DB-backed (me, progress, install, tree, onboarding, access, stripe-webhook stub)</name>
  <files>apps/web/app/api/me/route.ts, apps/web/app/api/progress/route.ts, apps/web/app/api/install/route.ts, apps/web/app/api/tree/route.ts, apps/web/app/api/onboarding/route.ts, apps/web/app/api/access/route.ts, apps/web/app/api/stripe/webhook/route.ts</files>
  <action>
    Handlers thin et uniformes, tous via `createServerClient()` de 04-01, scopés par la session (RLS applique auth.uid()). Chacun rejette 401 si pas de session (sauf access en PERSONAL_MODE, voir plus bas). Valider les bodies avec zod (déjà dep).
    - `GET /api/me` : lit la ligne `users` (paid, plan) + agrégats de progression (count progress done, count installs). Upsert la ligne `users` si absente (post-signup) à partir de auth.user. Réponse `{ id, email, paid, plan, installs, lessonsDone }`.
    - `POST /api/progress` : body `{ lessonId, status }` → upsert `progress (user_id=auth.uid(), lesson_id, status, completed_at)`. Réponse ligne mise à jour.
    - `POST /api/install` : body `{ slug }` → insert `installs (user_id, skill_slug)` (upsert idempotent). `DELETE` body `{ slug }` → delete. `GET` → liste des skill_slug installés (pour l'hydratation de useInstalls). Réponses `{ installed: string[] }`.
    - `POST /api/tree` : body `{ jobId, level }` (level ∈ job_level) → upsert `tree_state`. Réponse ligne. (GET catalogue = déjà servi par /api/catalog statique, NE PAS dupliquer ici.)
    - `GET/POST /api/onboarding` : GET lit `onboarding` (path, step, done) ; POST upsert `{ path?, step?, done? }`. (Câblage UI complet en Phase 5 ; ici l'endpoint DB existe.)
    - `GET /api/access` : renvoie `{ paid }`. Si `PERSONAL_MODE === "true"` → `paid:true` forcé (sans exiger la session, D8). Sinon lit `users.paid` de la session.
    - `POST /api/stripe/webhook` : STUB Stripe-ready. Sans `STRIPE_WEBHOOK_SECRET` → répond 501 `{ stripe:"not_configured" }` (jamais exercé en perso). Handler structurel documenté « brancher la vérif signature + flip users.paid quand Stripe live (EXT-02) ». AUCUNE dépendance `stripe` importée.
    Aucune de ces routes ne met de Cache-Control CDN (règle §6 : routes user jamais cachées).
  </action>
  <verify>
    `pnpm --filter web build` exit 0.
    Runtime (user authentifié de test) : `curl /api/me` → 200 avec {paid, installs} ; `curl -X POST /api/install -d '{"slug":"..."}'` → 200 puis `psql -c "select count(*) from installs where user_id=<A>"` = 1 ; `curl /api/access` avec PERSONAL_MODE=true → `{"paid":true}` ; `curl -X POST /api/stripe/webhook` sans secret → 501.
    `grep -rL "supabase" apps/web/app/api/catalog apps/web/app/api/my-tree` confirme les routes catalogue n'importent pas supabase (intactes).
  </verify>
  <done>7 routes user répondent, DB-backed, scopées auth.uid() ; install crée une vraie row installs ; access force paid en PERSONAL_MODE ; webhook stub 501 sans secret ; build vert ; routes catalogue intactes.</done>
</task>

<task type="auto">
  <name>Task 2: /api/brain (GET/PUT) + /api/brain/draft (gateway :8765, gated paid, rate-limit)</name>
  <files>apps/web/app/api/brain/route.ts, apps/web/app/api/brain/draft/route.ts</files>
  <action>
    `GET/PUT /api/brain` (createServerClient, 401 sans session) :
    - GET → les 8 lignes `brain` de l'user (section → {content, source}), défaut vide pour les sections absentes. Réponse conforme au type BrainMap consommé par useBrain().
    - PUT → body `{ section (brain_section), content, source (ai|manual) }` → upsert `brain (user_id, section, content, source, updated_at)`. Valider section ∈ BRAIN_SECTION_KEYS et source ∈ {ai,manual} (zod).
    `POST /api/brain/draft` — le SEUL point LLM :
    1. Exiger session + `paid` (403 `{error:"payment_required"}` si non-payé ; en PERSONAL_MODE paid est forcé donc un user authentifié passe).
    2. Rate-limit PAR USER : Map en mémoire module `Map<userId, timestamps[]>`, fenêtre glissante (ex. 5 drafts / 10 min) → au-delà, 429 `{error:"rate_limited"}`. (Store durable = upgrade multi-tenant, documenté.)
    3. Body `{ url?, notes? }` → construire un prompt FR qui demande les 8 sections du Brain à partir de l'input (mêmes clés que BRAIN_SECTION_KEYS).
    4. `fetch(`${process.env.HERMES_GATEWAY_URL}/api/llm/complete`, { method:"POST", headers:{ "Content-Type":"application/json", "X-Internal-Token": process.env.HERMES_INTERNAL_TOKEN }, body: JSON.stringify({ prompt, model:"balanced", max_tokens:1200, source:"skilltree-brain-draft" }) })`. AUCUN header x-api-key / Authorization Bearer / clé Anthropic.
    5. Parser `text` de la réponse gateway → mapper en `Record<BrainSectionKey, string>` (parsing robuste : sections délimitées ; fallback : mettre tout le texte dans `company` plutôt que d'inventer, jamais renvoyer le gabarit du stub). Répondre ce Record — c'est ce que `draftBrain()` (Task 3) attend.
    6. Si la gateway échoue (non-2xx / injoignable) → 502 explicite `{error:"draft_upstream"}` (PAS de fallback silencieux vers un gabarit : une panne LLM DOIT échouer, cf. règle anti-faux-positif).
  </action>
  <verify>
    `pnpm --filter web build` exit 0.
    Runtime (user payé de test, gateway :8765 up) : `curl -X POST /api/brain/draft -d '{"notes":"agence growth B2B"}'` → 200 avec 8 clés, texte NON-gabarit (grep -v "Brouillon à partir de") ; ET `sqlite3 ~/hermes-data/hermes.db "select count(*) from proxy_calls where source='skilltree-brain-draft'"` a incrémenté.
    Gating : même appel SANS session/paid → 403. Rate-limit : boucle N+1 appels → le (N+1)ᵉ renvoie 429.
    Secrets : `grep -riE "x-api-key|ANTHROPIC_API_KEY|sk-ant" apps/web/app/api/brain/draft/route.ts` = 0 ; `grep -q "X-Internal-Token" apps/web/app/api/brain/draft/route.ts`.
  </verify>
  <done>/api/brain persiste les 8 sections (DB) ; /api/brain/draft renvoie un draft RÉEL via gateway (proxy_calls +1, contenu non-gabarit), 403 si non-payé, 429 au-delà du rate-limit, 0 clé Anthropic dans le code.</done>
</task>

<task type="auto">
  <name>Task 3: Swap des 3 corps de stubs vers /api + re-couplage TreeAudit/MyTree + preuve verify_p6</name>
  <files>apps/web/lib/installs.ts, apps/web/lib/progress.ts, apps/web/lib/brain.ts, apps/web/components/tree/TreeAudit.tsx, apps/web/components/tree/MyTree.tsx, tools/verify_p6.py</files>
  <action>
    Swapper les CORPS (signatures EXACTES inchangées — cf. constraints) :
    - `lib/installs.ts` : `useInstalls()` hydrate via `GET /api/install` (au montage), `install()/uninstall()` appellent `POST/DELETE /api/install` puis mettent à jour l'état local optimiste. Conserver l'event `skilltree:installs` (dispatch après mutation) pour que tout hook monté se re-render — ainsi la réactivité inter-composants survit sans coupler l'UI. `isInstalled/install/uninstall` (fonctions pures hors hook) deviennent async OU gardent leur signature en tapant sur un cache module hydraté ; préférer garder la signature sync des fonctions pures en s'appuyant sur un cache module rafraîchi par le hook, et faire des mutations fire-and-forget vers l'API (documenter). Supprimer la persistance localStorage.
    - `lib/progress.ts` : `useProgress()` hydrate via une lecture DB (agrégée dans /api/me ou un GET dédié — réutiliser /api/me.installs/lessons ou ajouter la lecture dans /api/progress GET si nécessaire SANS changer la signature du hook) ; `markComplete()` → `POST /api/progress`. `isComplete/countDone` lisent le cache hydraté. Conserver l'event `skilltree:progress`.
    - `lib/brain.ts` : `useBrain()` hydrate via `GET /api/brain`, `save/saveAll` → `PUT /api/brain`. `draftBrain(input)` → `await fetch("/api/brain/draft", {method:"POST", body: JSON.stringify(input)})` et retourne le `Record<BrainSectionKey,string>` (le composant appelant utilise déjà le résultat de façon async côté Phase 3 — vérifier l'await ; ne pas changer la signature retournée). Supprimer le gabarit déterministe local et la persistance localStorage. `loadBrainMap()/firstUnfilledIndex()/BRAIN_SECTIONS/BRAIN_SECTION_KEYS` inchangés.
    - RE-COUPLAGE : `TreeAudit.tsx` + `MyTree.tsx` — remplacer la lecture du localStorage brut (`skilltree.installs.v1` / listener `skilltree:installs`) par `useInstalls()` (liste `installed` + `count`). Changer UNIQUEMENT la source de données et le calcul du compteur ; NE PAS toucher au JSX/markup. Vérifier que le compteur TreeAudit réagit toujours à une install sans reload (via le re-render du hook).
    Écrire `tools/verify_p6.py` (pattern verify_pN Playwright + assertions DB/SQLite, exit 1 au 1er FAIL, screenshots + txt dans orchestration/verify/p6/) prouvant les 4 critères de succès de la phase :
    1. PERSISTANCE : signup (ou login user test) → `/hub`, installer un skill via l'UI → `select count(*) from installs where user_id=<user>` passe de 0 à 1 → reload la page → le skill est toujours marqué installé (assert DOM "Installé"). Compteur DB avant/après consigné.
    2. CATALOGUE 0-DB : hit /api/catalog → 200 ; répéter avec Supabase coupé (ou en asserttant l'absence d'import supabase) → toujours 200 ; consigner.
    3. DRAFT réel : appeler /api/brain/draft (user payé) → 200, 8 sections, texte ≠ gabarit ; `proxy_calls` +1 (source skilltree-brain-draft) ; gating 403 sans paid ; 429 au-delà du rate-limit.
    4. ACCESS : /api/access en PERSONAL_MODE → {paid:true}.
    Bonus assert (constraint fidélité) : `git diff --name-only` sur apps/web/components montre UNIQUEMENT tree/TreeAudit.tsx + tree/MyTree.tsx (aucun autre composant présentationnel modifié).
  </action>
  <verify>
    `pnpm --filter web build` exit 0 ; `pnpm --filter web tsc --noEmit` propre.
    `grep -c "localStorage" apps/web/lib/installs.ts apps/web/lib/brain.ts` = 0 (persistance localStorage retirée) ; `grep -q "/api/install" apps/web/lib/installs.ts` ; `grep -q "/api/brain/draft" apps/web/lib/brain.ts` ; `grep -q "useInstalls" apps/web/components/tree/TreeAudit.tsx apps/web/components/tree/MyTree.tsx`.
    Signatures : `grep -E "export function (install|uninstall|isInstalled|useInstalls|draftBrain|loadBrainMap)" apps/web/lib/*.ts` présent, arité inchangée.
    `git diff --name-only apps/web/components` = uniquement tree/TreeAudit.tsx + tree/MyTree.tsx.
    `python tools/verify_p6.py` → tous PASS, exit 0, écrit orchestration/verify/p6/{install-persist.png, dom-assert.txt, draft-proof.txt, access.txt}.
  </verify>
  <done>Les 3 stubs tapent sur /api (0 localStorage de persistance), signatures identiques, UI présentationnelle 0 diff sauf TreeAudit/MyTree re-couplés sur useInstalls() ; verify_p6.py prouve persistance DB, catalogue 0-DB, draft réel gateway, access PERSONAL_MODE — tout dans orchestration/verify/p6/.</done>
</task>

</tasks>

<verification>
- Persistance : signup → install → reload → installé, avec row Postgres installs (compteur avant/après) — pas localStorage.
- Routes user me/progress/install/tree/brain/onboarding/access DB-backed, scopées auth.uid() ; catalogue (/api/catalog, /api/skills/[slug], /api/my-tree) 0-DB (0 import supabase + 200 DB coupée).
- /api/brain/draft : draft réel gateway :8765 (proxy_calls +1, texte non-gabarit), gated paid (403), rate-limit (429), 0 clé Anthropic dans le code.
- /api/access PERSONAL_MODE → paid:true ; webhook Stripe stub 501 sans secret.
- Swap : signatures figées, UI 0 diff hors TreeAudit/MyTree re-couplés sur useInstalls().
- Preuves dans orchestration/verify/p6/ ; build vert + tsc propre.
</verification>

<success_criteria>
- Success criterion phase #2 : parcours signup→install→reload persistant (row Postgres, compteur).
- #3 : les 7 routes user remplacent les stubs ; catalogue = 0 requête DB (assert).
- #4 : /api/brain/draft draft réel via gateway (log proxy_calls + non-gabarit), gated paid, refuse au-delà du rate-limit ; /api/access Stripe-ready derrière PERSONAL_MODE=true.
- Stub swap sans réécriture UI (signatures identiques, seul TreeAudit/MyTree re-couplés — signalé).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-backend/04-02-SUMMARY.md` (inclure : la décision rate-limit en mémoire process + son upgrade path durable, le re-couplage TreeAudit/MyTree signalé, la confirmation 0-clé-Anthropic dans le code, et les chemins des preuves orchestration/verify/p6/).
</output>
