---
phase: 04-backend
plan: 01
type: execute
wave: 1
depends_on: []
model: opus-4-8
effort: high
autonomous: true
files_modified:
  - supabase/schema.sql
  - supabase/config.toml
  - supabase/tests/rls_isolation.mjs
  - apps/web/lib/supabase/server.ts
  - apps/web/lib/supabase/client.ts
  - apps/web/middleware.ts
  - apps/web/app/auth/login/page.tsx
  - apps/web/app/auth/callback/route.ts
  - apps/web/app/auth/signout/route.ts
  - apps/web/package.json
  - apps/web/.env.example
user_setup:
  - service: supabase
    why: "State user réel (Postgres + Auth + RLS). Perso = stack Supabase LOCAL (docker), zéro compte externe requis. Un projet hébergé n'est nécessaire qu'au déploiement (hors périmètre P4)."
    env_vars:
      - name: SUPABASE_URL
        source: "supabase start (sortie: API URL, ex. http://127.0.0.1:54321) — écrit dans apps/web/.env.local"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "supabase start (sortie: anon key)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "supabase start (sortie: service_role key) — SERVER-ONLY, jamais exposé au client"

must_haves:
  truths:
    - "supabase/schema.sql crée EXACTEMENT les 6 tables users/progress/installs/brain/tree_state/onboarding + les enums lesson_status/brain_section/brain_source/job_level/onboarding_path, conformes à ARCHITECTURE §5 (aucune table du prototype obsolète : ni sectors/skills/skill_edges)."
    - "RLS est ACTIVÉE (enable row level security) sur les 6 tables user, avec une policy user_id = auth.uid() (users : id = auth.uid())."
    - "Un utilisateur B authentifié qui SELECT les tables user ne voit AUCUNE ligne de l'utilisateur A (0 ligne), alors qu'un client service-role voit bien la ligne de A (elle existe) — l'isolation vient du RLS, pas d'une table vide."
    - "Le test d'isolation vire au ROUGE si le RLS est retiré (B verrait la ligne de A) — c'est tout l'intérêt du test : un PASS sur un schéma sans RLS = fuite de données."
    - "Un utilisateur peut s'inscrire (email/password), la session persiste au reload (cookies rafraîchis par middleware) ; la route de callback magic-link existe et échange le code contre une session."
  artifacts:
    - path: "supabase/schema.sql"
      provides: "6 tables user + 5 enums + RLS activé + policies (REMPLACE l'ancien schema.sql prototype)"
      contains: "create table users"
    - path: "supabase/tests/rls_isolation.mjs"
      provides: "Preuve d'isolation tenant multi-table : service-role voit la ligne de A, client B authentifié voit 0 ligne de A ; exit 1 au premier échec ; écrit orchestration/verify/p6/rls-isolation.txt"
      min_lines: 60
    - path: "apps/web/lib/supabase/server.ts"
      provides: "Client Supabase server-side (cookies, @supabase/ssr) + helper service-role server-only"
      exports: ["createServerClient", "createServiceClient"]
    - path: "apps/web/lib/supabase/client.ts"
      provides: "Client Supabase navigateur (@supabase/ssr createBrowserClient)"
    - path: "apps/web/middleware.ts"
      provides: "Rafraîchit la session Supabase à chaque requête (session persistante)"
    - path: "apps/web/app/auth/login/page.tsx"
      provides: "Écran login FR : email/password + magic link"
    - path: "apps/web/app/auth/callback/route.ts"
      provides: "Échange le code magic-link contre une session (exchangeCodeForSession)"
  key_links:
    - from: "supabase/schema.sql RLS policies"
      to: "auth.uid()"
      via: "using (user_id = auth.uid())"
      pattern: "auth\\.uid\\(\\)"
    - from: "apps/web/middleware.ts"
      to: "session Supabase (cookies)"
      via: "supabase.auth.getUser() + set cookies"
      pattern: "auth\\.getUser|updateSession"
    - from: "supabase/tests/rls_isolation.mjs"
      to: "installs/progress/brain/tree_state/onboarding (as user B)"
      via: "SELECT authentifié B → 0 ligne de A"
      pattern: "auth-uid|user_id|isolation"
---

<objective>
Poser la fondation state-user irréversible de SkillTree-OS : le schéma Postgres/Supabase conforme à ARCHITECTURE §5 (6 tables user + enums), le RLS par-user (D5) sur TOUTES les tables, l'auth Supabase (email/password + magic link, session persistante), et les clients Supabase (server/browser/middleware) que le Plan 04-02 branchera sur les routes. Ce plan REMPLACE `supabase/schema.sql` (obsolète — il modèle l'ancien prototype sectors/skills/skill_edges, cf. ARCHITECTURE §0) et NE crée AUCUNE route user (celles-ci appartiennent à 04-02).

Purpose : R13 sécurité d'abord + décision irréversible (un schéma + des policies RLS déployés se migrent douloureusement). Le RLS doit être correct AU PREMIER JET, prouvé par un test d'isolation qui échoue si le RLS est retiré (anti-faux-positif : un test vert sur un schéma sans RLS = fuite de données, exactement ce qu'on interdit).

Output : supabase/schema.sql (6 tables + 5 enums + RLS + policies), les 3 clients Supabase (server/client/middleware), l'auth (login page + callback + signout), et la preuve d'isolation dans orchestration/verify/p6/rls-isolation.txt.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Contrat de schéma + décisions (source de vérité — le schéma DOIT être copié depuis §5)
@docs/ARCHITECTURE.md

# Ancien schéma à REMPLACER intégralement (modèle prototype obsolète — NE PAS conserver ses tables)
@supabase/schema.sql

# Règle d'auth OAuth vs clé API (contexte : le Brain draft de 04-02 relaie l'OAuth via la gateway,
# JAMAIS de clé Anthropic dans l'app — ce plan ne touche pas au LLM mais fixe la posture secrets)
@/home/nuveo/.claude/CLAUDE.md
</context>

<constraints>
- Le schéma est la COPIE FIDÈLE d'ARCHITECTURE §5 : 6 tables `users, progress, installs, brain, tree_state, onboarding`, enums `lesson_status, brain_section, brain_source, job_level, onboarding_path`. AUCUNE table catalogue en DB (D4 : le catalogue reste des fichiers buildés). Supprimer entièrement l'ancien contenu de `supabase/schema.sql` (sectors/skills/skill_edges/skill_status/skill_stage n'existent plus).
- RLS sur les 6 tables. `users` : policy `id = auth.uid()`. Les 5 autres : `user_id = auth.uid()` (select/insert/update/delete). `enable row level security` + `force` non requis mais policies explicites obligatoires.
- Perso = Supabase LOCAL via CLI (`supabase init` déjà OK si config.toml ; sinon l'initialiser) + `supabase start` (docker présent). Récupérer API URL + anon + service_role depuis la sortie de `supabase start` et les écrire dans `apps/web/.env.local` (git-ignoré). `.env.example` (committé) documente les clés SANS valeur.
- Auth server-side avec `@supabase/ssr` (createServerClient + cookies) — PAS l'ancien `@supabase/auth-helpers` (déprécié). Le service-role key est SERVER-ONLY (jamais `NEXT_PUBLIC_`). L'anon key est `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
- Ne crée AUCUN fichier sous `apps/web/app/api/**` (propriété de 04-02) ni ne modifie `apps/web/lib/{installs,progress,brain}.ts` (propriété de 04-02). Zéro chevauchement de fichiers.
- Anti-faux-positif : ne JAMAIS conclure « RLS OK » sur un `exit 0` de migration. La preuve = un client B authentifié qui lit 0 ligne de A pendant qu'un client service-role lit bien la ligne de A.
- NOTE EXÉCUTEUR (non bloquante) : la Phase 3 a montré que des exécuteurs parallèles écrasent `apps/web/.next` + l'index git. 04-01 et 04-02 sont séquentiels (wave 1 → 2) donc risque faible ; en cas d'exécution concurrente, isoler par worktree.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: schema.sql conforme §5 + RLS sur les 6 tables + stack Supabase local appliqué</name>
  <files>supabase/schema.sql, supabase/config.toml, apps/web/.env.example</files>
  <action>
    REMPLACER entièrement `supabase/schema.sql` par le schéma d'ARCHITECTURE §5, copié fidèlement :
    - `create table users (id uuid primary key references auth.users, email text not null, paid bool not null default false, plan text not null default 'member', created_at timestamptz not null default now())`.
    - `create type lesson_status as enum ('locked','in_progress','done')` + table `progress (user_id, lesson_id text, status, completed_at, primary key(user_id, lesson_id))`.
    - table `installs (user_id, skill_slug text, installed_at, primary key(user_id, skill_slug))`.
    - `create type brain_section as enum ('company','offer','customers','voice','ops','stack','goals','constraints')` + `create type brain_source as enum ('ai','manual')` + table `brain (user_id, section brain_section, content text default '', source brain_source default 'manual', updated_at, primary key(user_id, section))`.
    - `create type job_level as enum ('none','manual','assisted','autonomous')` + table `tree_state (user_id, job_id text, level job_level default 'none', updated_at, primary key(user_id, job_id))`.
    - `create type onboarding_path as enum ('agency','business','company','exploring')` + table `onboarding (user_id uuid primary key, path onboarding_path, step int default 0, done bool default false)`.
    - Tous les `user_id` référencent `users(id)`.
    Puis, dans le MÊME fichier, ACTIVER le RLS et poser les policies :
    - `alter table <t> enable row level security;` pour les 6 tables.
    - `users` : policy select/update `using (id = auth.uid())`, insert `with check (id = auth.uid())`.
    - `progress/installs/brain/tree_state/onboarding` : policies select/insert/update/delete `using (user_id = auth.uid())` et `with check (user_id = auth.uid())`.
    - Un trigger/or une note : la ligne `users` est créée à l'inscription (on la crée côté route `/api/me` en 04-02, ou via un trigger `on auth.users` — préférer le trigger `handle_new_user` inséré ici pour que la ligne users existe dès signup ; sinon documenter clairement que 04-02 upsert la ligne users).
    AUCUNE table catalogue (D4). Supprimer toute trace de sectors/skills/skill_edges.

    Stack local : si `supabase/config.toml` absent → `pnpm dlx supabase init` (ou binaire supabase). `supabase start` (docker présent) → récupérer API URL + anon + service_role. Appliquer le schéma : `psql "$DB_URL" -f supabase/schema.sql` OU `supabase db reset` avec le schéma en migration. Écrire `apps/web/.env.local` (git-ignoré) : SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, PERSONAL_MODE=true, HERMES_GATEWAY_URL=http://localhost:8765, HERMES_INTERNAL_TOKEN=<copié depuis ~/hermes-proxy/.env>. Écrire `apps/web/.env.example` (committé) avec ces mêmes clés SANS valeur + commentaires. Configurer l'auto-confirm email en local (config.toml `[auth.email] enable_confirmations = false`) pour rendre le parcours signup testable sans lien mail.
  </action>
  <verify>
    `psql "$DB_URL" -c "\dt"` liste EXACTEMENT users, progress, installs, brain, tree_state, onboarding (aucune table catalogue).
    `psql "$DB_URL" -c "select tablename, rowsecurity from pg_tables where schemaname='public'"` → rowsecurity = true pour les 6.
    `psql "$DB_URL" -c "select count(*) from pg_policies where schemaname='public'"` ≥ 6.
    `grep -c "create type" supabase/schema.sql` = 5 ; `grep -c "auth.uid()" supabase/schema.sql` ≥ 6 ; `grep -i "create table skills\|skill_edges\|create table sectors" supabase/schema.sql` = 0.
  </verify>
  <done>Les 6 tables + 5 enums d'ARCHITECTURE §5 existent en base locale, RLS actif sur les 6, ≥6 policies auth.uid(), aucune table catalogue, .env.local peuplé + .env.example committé.</done>
</task>

<task type="auto">
  <name>Task 2: Clients Supabase (server/browser/middleware) + Auth email-password + magic link</name>
  <files>apps/web/lib/supabase/server.ts, apps/web/lib/supabase/client.ts, apps/web/middleware.ts, apps/web/app/auth/login/page.tsx, apps/web/app/auth/callback/route.ts, apps/web/app/auth/signout/route.ts, apps/web/package.json</files>
  <action>
    Ajouter les deps : `pnpm --filter web add @supabase/supabase-js @supabase/ssr` (modifie apps/web/package.json + lockfile).
    `lib/supabase/server.ts` : `createServerClient()` (@supabase/ssr, lecture/écriture cookies via next/headers) utilisant SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY ; + `createServiceClient()` server-only utilisant SUPABASE_SERVICE_ROLE_KEY (garde-fou : throw si appelé côté client / si la clé absente). Ces helpers seront importés par les routes de 04-02 — signatures stables.
    `lib/supabase/client.ts` : `createBrowserClient()` (@supabase/ssr) avec l'anon key publique.
    `middleware.ts` (racine apps/web) : `updateSession` qui rafraîchit les cookies de session Supabase à chaque requête (pattern @supabase/ssr officiel), matcher excluant _next/static et les assets. C'est ce qui rend la session PERSISTANTE au reload.
    `app/auth/login/page.tsx` : écran FR ("use client") avec deux chemins — (a) email + password → `supabase.auth.signInWithPassword` / lien "créer un compte" → `signUp` ; (b) "Recevoir un lien magique" → `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: <origin>/auth/callback } })`. Redirige vers `/hub` (ou `/`) après succès. Pas de refonte de style : réutiliser les tokens/classes existants du shell.
    `app/auth/callback/route.ts` : GET qui lit `?code=`, `supabase.auth.exchangeCodeForSession(code)`, puis redirige. (Sous app/auth — PAS app/api, aucun chevauchement avec 04-02.)
    `app/auth/signout/route.ts` : POST `supabase.auth.signOut()` → redirige /auth/login.
    NE PAS toucher aux routes existantes /api/catalog, /api/my-tree, /api/skills/[slug].
  </action>
  <verify>
    `pnpm --filter web build` exit 0 (typecheck des clients + auth pages OK).
    `pnpm --filter web tsc --noEmit` propre.
    `grep -q "createServerClient" apps/web/lib/supabase/server.ts && grep -q "SERVICE_ROLE" apps/web/lib/supabase/server.ts` (service-role présent, server-only).
    `grep -q "signInWithOtp" apps/web/app/auth/login/page.tsx && grep -q "signInWithPassword" apps/web/app/auth/login/page.tsx` (2 chemins auth).
    `grep -q "exchangeCodeForSession" apps/web/app/auth/callback/route.ts`.
    Runtime : app démarrée, POST signup d'un email test → `psql -c "select count(*) from auth.users"` incrémente ; reload d'une page protégée conserve la session (cookie sb-*).
  </verify>
  <done>Clients server/browser/middleware fonctionnels, login email/password + magic link opérationnels, callback échange le code, build vert, session persiste au reload (vérifié DB auth.users + cookie).</done>
</task>

<task type="auto">
  <name>Task 3: Preuve d'isolation tenant multi-table (ROUGE sans RLS)</name>
  <files>supabase/tests/rls_isolation.mjs</files>
  <action>
    Écrire `supabase/tests/rls_isolation.mjs` (Node, `@supabase/supabase-js`), preuve réelle sur données réelles :
    1. Charge SUPABASE_URL / anon / service_role depuis apps/web/.env.local.
    2. Admin (service-role) crée 2 users A et B (auth.admin.createUser, email_confirm:true) + insère leurs lignes `users`.
    3. Deux clients ANON authentifiés : signIn A, signIn B (chacun sa session/JWT).
    4. Pour CHAQUE table user (progress, installs, brain, tree_state, onboarding) : insérer UNE ligne appartenant à A via le client A (RLS autorise l'écriture de sa propre ligne).
    5. CONTRÔLE POSITIF : le client service-role (bypass RLS) `select` sur chaque table `where user_id = A` → DOIT retourner 1 (la ligne existe vraiment).
    6. ASSERTION D'ISOLATION : le client B authentifié `select *` sur chaque table → DOIT retourner 0 ligne appartenant à A (B ne voit que ses propres lignes = 0). Compter explicitement les lignes A visibles par B ; attendu = 0 pour les 5 tables.
    7. Design ROUGE-sans-RLS (à écrire en commentaire ET à respecter dans la logique) : si le RLS était retiré, l'étape 6 verrait la ligne de A → l'assert échoue → exit 1. Un PASS n'est possible QUE si le RLS masque effectivement A à B.
    Sortie : chaque check imprime PASS/FAIL + le compte mesuré ; premier FAIL → `process.exit(1)` (aucune preuve verte consignée sur échec). Tout PASS → écrire `orchestration/verify/p6/rls-isolation.txt` (récap : par table, service-role=1 / B-voit-A=0) et nettoyer les users de test.
  </action>
  <verify>
    `node supabase/tests/rls_isolation.mjs` → imprime 5× "service_role sees A = 1" et 5× "B sees A's rows = 0", exit 0, et crée `orchestration/verify/p6/rls-isolation.txt`.
    Contre-preuve documentée (à exécuter manuellement une fois, PAS committée activée) : `alter table installs disable row level security;` puis relancer → le test DOIT passer en exit 1 (B voit la ligne de A). Réactiver ensuite. Consigner l'observation dans le SUMMARY.
  </verify>
  <done>Le test prouve l'isolation sur les 5 tables user (service-role voit A, B ne voit pas A), exit 0 avec RLS, et vire au rouge (exit 1) si le RLS est retiré ; preuve dans orchestration/verify/p6/rls-isolation.txt.</done>
</task>

</tasks>

<verification>
- Schéma : 6 tables + 5 enums conformes §5, 0 table catalogue, RLS actif sur les 6 (pg_tables.rowsecurity), ≥6 policies auth.uid().
- Auth : signup email/password → session persistante au reload (auth.users incrémenté, cookie sb-*), magic-link callback échange le code.
- Isolation : `node supabase/tests/rls_isolation.mjs` exit 0 (B voit 0 ligne de A, service-role voit A) ; rouge si RLS retiré.
- Build : `pnpm --filter web build` exit 0, tsc propre.
- Zéro fichier sous apps/web/app/api/** ni lib/{installs,progress,brain}.ts touché (propriété 04-02).
</verification>

<success_criteria>
- ARCHITECTURE §5 reproduit à l'identique (6 tables, 5 enums), ancien schema.sql prototype supprimé.
- RLS `user_id = auth.uid()` (users : `id = auth.uid()`) sur les 6 tables, prouvé par un test d'isolation qui échoue sans RLS.
- Auth Supabase email/password + magic link, session persistante (middleware).
- Clients server/browser/service-role prêts pour 04-02, build vert.
- Preuve dans orchestration/verify/p6/rls-isolation.txt.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/04-backend/04-01-SUMMARY.md` (inclure : l'observation de la contre-preuve RLS-désactivé = test rouge, les valeurs URL/keys locales NON committées, et le rappel que 04-02 upsert/lit les lignes users via les clients ici posés).
</output>
