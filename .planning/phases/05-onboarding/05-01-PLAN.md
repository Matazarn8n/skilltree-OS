---
phase: 05-onboarding
plan: 01
type: execute
model: sonnet-4-6
effort: medium
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/onboarding.ts
  - apps/web/components/onboarding/OnboardingFlow.tsx
  - apps/web/components/onboarding/TourTooltip.tsx
  - apps/web/app/(app)/onboarding/page.tsx
  - apps/web/app/page.tsx
  - apps/web/.env.example
  - apps/web/.env.local
  - tools/verify_p7.py
autonomous: true

must_haves:
  truths:
    - "Un nouvel utilisateur voit le tour guidé avec overlay « Étape X sur 6 » et peut le parcourir de 1 à 6."
    - "Choisir un parcours (agency/business/company/exploring) écrit path dans la row onboarding (Postgres), pas seulement en state client."
    - "Chaque avancement d'étape écrit step dans la row onboarding ; recharger à l'étape 3 reprend à 3 (lu depuis la DB), pas à 1."
    - "« Passer » ou terminer l'étape 6 écrit done=true ; au reload le tour ne se ré-affiche plus."
    - "Un CTA Cal.com « Strategy Call » (lien externe href = NEXT_PUBLIC_CALCOM_URL) est présent en fin de parcours."
    - "Landing post-login onboarding-aware : un utilisateur done=false qui atterrit sur la racine est envoyé vers /onboarding ; done=true → /map."
  artifacts:
    - path: "apps/web/lib/onboarding.ts"
      provides: "Client de persistance onboarding (cache module + hook useOnboarding + GET/POST /api/onboarding), signatures propres façon lib/installs.ts"
      contains: "api/onboarding"
      min_lines: 40
    - path: "apps/web/components/onboarding/OnboardingFlow.tsx"
      provides: "Tour 6 étapes persistant + 4 parcours mappés à l'enum onboarding_path + reprise au step + CTA Cal.com"
      contains: "NEXT_PUBLIC_CALCOM_URL"
      min_lines: 120
    - path: "apps/web/app/(app)/onboarding/page.tsx"
      provides: "Server gate : lit la row onboarding, redirige vers /map si done. La reprise à l'étape N est assurée par la ré-hydratation client de useOnboarding() (bref rendu !loaded avant hydratation ; SSR anti-flash = raffinement optionnel via props initialStep)"
      contains: "redirect"
    - path: "apps/web/app/page.tsx"
      provides: "Landing post-login onboarding-aware (racine → /onboarding si done=false, sinon /map)"
      contains: "onboarding"
    - path: "tools/verify_p7.py"
      provides: "Preuve Playwright : row onboarding réelle (psql), reprise au reload, done, CTA Cal.com, landing"
      contains: "supabase_db_skilltree-OS"
      min_lines: 120
  key_links:
    - from: "apps/web/components/onboarding/OnboardingFlow.tsx"
      to: "apps/web/lib/onboarding.ts"
      via: "useOnboarding() hook (hydrate + setPath/setStep/complete)"
      pattern: "useOnboarding"
    - from: "apps/web/lib/onboarding.ts"
      to: "/api/onboarding"
      via: "GET hydrate + POST persist"
      pattern: "fetch\\(\"/api/onboarding\""
    - from: "apps/web/app/(app)/onboarding/page.tsx"
      to: "onboarding row"
      via: "server client read → redirect si done"
      pattern: "from\\(\"onboarding\"\\)"
    - from: "apps/web/app/page.tsx"
      to: "onboarding row"
      via: "landing gate serveur"
      pattern: "redirect\\("
---

<objective>
Livrer l'onboarding v1 de SkillTree-OS : un tour guidé 6 étapes fidèle à `captures/dynamic/`, avec sélection de parcours (agency/business/company/exploring), CTA Cal.com « Strategy Call », le tout PERSISTÉ côté serveur via la route `/api/onboarding` déjà livrée en Phase 4.

Purpose: L'onboarding existant (Phase 3, `OnboardingFlow.tsx`) est un stepper `useState` PUR — il oublie tout au reload, n'a pas de Cal.com, et ses 4 « motives » ne correspondent pas à l'enum `onboarding_path` de la DB. Phase 5 le fait ÉVOLUER (pas réécrire) pour : (a) persister l'avancement (reprise au reload prouvée par une VRAIE row Postgres), (b) mapper les 4 parcours à l'enum, (c) ajouter le CTA Cal.com externe, (d) rendre la landing post-login onboarding-aware. Satisfait ONB-01 (tour 6 étapes persistant, skip → done) et ONB-02 (parcours + Cal.com).

Output:
- `apps/web/lib/onboarding.ts` (nouveau, calqué sur `lib/installs.ts`)
- `OnboardingFlow.tsx` / `TourTooltip.tsx` évolués (persistance + parcours enum + Cal.com + testids)
- `(app)/onboarding/page.tsx` server gate + `app/page.tsx` landing gate
- `tools/verify_p7.py` + preuves dans `orchestration/verify/p7/`
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Contrat backend Phase 4 (À CONSOMMER, NE PAS RETOUCHER) — GET → {path,step,done}, POST upsert {path?,step?,done?}, scopé auth.uid()
@apps/web/app/api/onboarding/route.ts

# Pattern de swap localStorage→/api établi en Phase 4 : cache module + hydrate() GET + POST fire-and-forget + hook useX(). RÉFÉRENCE DE STYLE à mirrorer pour lib/onboarding.ts.
@apps/web/lib/installs.ts

# À faire évoluer (Phase 3), PAS réécrire from scratch
@apps/web/components/onboarding/OnboardingFlow.tsx
@apps/web/components/onboarding/TourTooltip.tsx
@apps/web/app/(app)/onboarding/page.tsx

# Server client + entrée / login (pour le gate de landing)
@apps/web/lib/supabase/server.ts
@apps/web/app/page.tsx

# Harness de preuve de référence (Phase 4) : psql docker, compteurs réels, exit-1 au 1er FAIL, aucune preuve verte écrite sur échec
@tools/verify_p6.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/onboarding.ts (nouveau) + câblage persistant de OnboardingFlow (4 parcours enum, reprise au step, Cal.com)</name>
  <files>apps/web/lib/onboarding.ts, apps/web/components/onboarding/OnboardingFlow.tsx, apps/web/components/onboarding/TourTooltip.tsx</files>
  <action>
Créer `apps/web/lib/onboarding.ts` en MIRRORANT strictement le pattern de `lib/installs.ts` (cache module + `hydrate()` via GET + mutation POST fire-and-forget + hook `useOnboarding()`), signatures propres :
- État module : `let cache: { path: OnboardingPath | null; step: number; done: boolean }` initialisé `{path:null, step:0, done:false}`. `type OnboardingPath = "agency" | "business" | "company" | "exploring"` (exporté, EXACTEMENT l'enum de la route Phase 4 — pas de valeurs inventées).
- `async function hydrate()` : `fetch("/api/onboarding", { cache: "no-store" })` ; si ok, `cache = { path, step, done }` puis `emit()` via un `Event("skilltree:onboarding")`. Try/catch silencieux (non authentifié/offline → cache inchangé, PAS d'invention).
- Mutations, chacune met à jour le cache optimiste + `emit()` + POST fire-and-forget `fetch("/api/onboarding", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify(patch) })` :
  - `setPath(path)` → POST `{ path, step: 1 }` (choisir un parcours démarre le tour à l'étape 1).
  - `setStep(step)` → POST `{ step }`.
  - `complete()` → cache.done=true, POST `{ done: true }`.
- Hook `useOnboarding()` : hydrate au montage + écoute l'event, expose `{ path, step, done, loaded, setPath, setStep, complete }`. `loaded` passe true après la 1re hydratation (pour éviter d'afficher « welcome » avant de savoir si un parcours existe déjà).

Faire ÉVOLUER `OnboardingFlow.tsx` (garder la structure/tokens visuels, ne pas repartir de zéro) :
- Remplacer le `type Motive` bancal (`build-for-me`/`bring-ai-in`) par `OnboardingPath` importé de `lib/onboarding.ts`. Garder les 4 libellés FR mais mapper aux valeurs enum : `agency`, `business` (« Faire construire de l'IA pour mon business »), `company` (« Amener l'IA dans mon entreprise »), `exploring`. Ajouter `data-testid={`onb-path-${id}`}` sur chaque bouton de parcours.
- Remplacer les `useState` locaux (`phase`/`motive`/`step`) par la source de vérité `useOnboarding()` :
  - Tant que `!loaded` → ne rien afficher (ou spinner discret), pour éviter le flash.
  - `done === true` → afficher directement la phase « reveal » (voir Cal.com ci-dessous), pas le welcome.
  - `path === null` → phase « welcome » (choix de parcours). `chooseMotive(id)` appelle `setPath(id)`.
  - `path` défini + `step >= 1` + `!done` → phase « tour », rendu à `step` (REPRISE : le step vient de la DB via le hook, pas d'un useState réinitialisé à 1).
  - `nextStep()` : si `step < 6` → `setStep(step + 1)` ; si `step >= 6` → `complete()` puis phase reveal.
  - `onSkip` du TourTooltip → `complete()` puis reveal (skip → done, cf. ONB-01).
- Garder les 6 `TOUR_STEPS` existants (déjà 6) — vérifier/ajuster les libellés à la trame ARCHITECTURE §6 « map → path → open a job → that's the skill » (la carte / le chemin / ouvrir un métier / c'est le skill / ton Brain / mon arbre). Le total passé à TourTooltip DOIT être 6.
- Phase « reveal » : ajouter le CTA Cal.com « Strategy Call ». `const CALCOM_URL = process.env.NEXT_PUBLIC_CALCOM_URL || "https://cal.com/skilltree/strategy-call";` puis un `<a href={CALCOM_URL} target="_blank" rel="noopener noreferrer" data-testid="onb-calcom-cta">` stylé comme un bouton (réutiliser les tokens du Button). PAS de widget/SDK Cal.com, PAS de dépendance npm (ponytail : pas de compte Cal.com câblé, URL = placeholder configurable). Garder aussi le CTA existant « Montre-moi le chemin agence → » vers `/map`.

`TourTooltip.tsx` : ajouter `data-testid="onb-step-indicator"` sur le `<p>` « Étape n sur total » (pour que la preuve lise le numéro d'étape rendu) ET `data-testid="onb-next"` sur le `<Button>` primaire (celui dont le label bascule Suivant/Terminer) — pour que la preuve clique par testid quel que soit le label. Ne rien changer d'autre.

Anti-faux-positif : la reprise NE DOIT PAS reposer sur un `useState(1)` — elle lit `step` depuis `useOnboarding()` (donc depuis `/api/onboarding` → row DB). Ne PAS toucher `route.ts` ni les autres lib swappées (installs/progress/brain).
  </action>
  <verify>
`cd apps/web && npx tsc --noEmit` exit 0 (typecheck strict, aucune erreur).
`grep -n "useOnboarding" apps/web/components/onboarding/OnboardingFlow.tsx` → présent.
`grep -n "NEXT_PUBLIC_CALCOM_URL" apps/web/components/onboarding/OnboardingFlow.tsx` → présent.
`grep -n "\"agency\"\|\"business\"\|\"company\"\|\"exploring\"" apps/web/lib/onboarding.ts` → les 4 valeurs enum présentes, aucune valeur hors-enum (`build-for-me`/`bring-ai-in` absents de OnboardingFlow.tsx).
`grep -n "onb-step-indicator\|onb-path-\|onb-calcom-cta" apps/web/components/onboarding/*.tsx` → les 3 testids présents.
  </verify>
  <done>
`lib/onboarding.ts` existe, calqué sur installs.ts (cache + hydrate GET + POST fire-and-forget + hook). OnboardingFlow consomme `useOnboarding()`, propose 4 parcours mappés à l'enum, un tour de 6 étapes qui reprend au `step` persisté, un CTA Cal.com externe. Typecheck vert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server gate onboarding (resume/redirect) + landing post-login + env NEXT_PUBLIC_CALCOM_URL</name>
  <files>apps/web/app/(app)/onboarding/page.tsx, apps/web/app/page.tsx, apps/web/.env.example, apps/web/.env.local</files>
  <action>
Convertir `apps/web/app/(app)/onboarding/page.tsx` en Server Component qui lit la row onboarding avant de rendre :
- `const supabase = await createServerClient();` puis `supabase.auth.getUser()`.
- Si user présent : `const { data } = await supabase.from("onboarding").select("path, step, done").eq("user_id", user.id).maybeSingle();`
  - Si `data?.done === true` → `redirect("/map")` (ne pas re-montrer le tour à un utilisateur qui l'a terminé).
- Rendre `<OnboardingFlow />` (le composant reste client et re-hydrate côté client ; le gate serveur évite surtout le cas done=true). Import `redirect` depuis `next/navigation`, `createServerClient` depuis `@/lib/supabase/server`. Garder l'export `metadata`.

Landing post-login onboarding-aware — `apps/web/app/page.tsx` (racine, actuellement `redirect("/map")`) :
- En Server Component : lire `createServerClient()` + `auth.getUser()`.
- Utilisateur non connecté OU `onboarding.done === true` (ou pas de row mais on choisit /map par défaut pour un visiteur) → `redirect("/map")`.
- Utilisateur connecté avec `done !== true` → `redirect("/onboarding")`.
- Concrètement : lire la row `onboarding` comme ci-dessus ; `redirect(user && data && data.done !== true ? "/onboarding" : "/map")` — un utilisateur fraîchement inscrit (pas encore de row, done implicitement false) doit AUSSI atterrir sur /onboarding : traiter `!data` comme done=false quand `user` existe. Garder le fichier minimal (server, pas de "use client").

Env :
- `.env.example` : ajouter sous une section onboarding `NEXT_PUBLIC_CALCOM_URL=` avec un commentaire « URL publique Cal.com du Strategy Call (placeholder configurable ; pas de widget/SDK) ».
- `.env.local` : ajouter `NEXT_PUBLIC_CALCOM_URL=https://cal.com/skilltree/strategy-call` AVANT le build (les vars `NEXT_PUBLIC_` sont inlinées au build — sans ça le href du CTA tomberait sur le fallback code, ce qui reste valide mais on veut prouver l'inlining env).

Ne PAS toucher `route.ts`, ni le login (`app/auth/login/page.tsx`), ni les autres routes Phase 4.
  </action>
  <verify>
`cd apps/web && npx tsc --noEmit` exit 0.
`grep -n "from(\"onboarding\")\|redirect(" apps/web/app/(app)/onboarding/page.tsx` → lecture de la row + redirect présents.
`grep -n "redirect\|onboarding" apps/web/app/page.tsx` → gate de landing présent.
`grep -n "NEXT_PUBLIC_CALCOM_URL" apps/web/.env.example apps/web/.env.local` → présent dans les deux.
  </verify>
  <done>
`/onboarding` redirige vers `/map` quand `done=true`. La racine `/` envoie un utilisateur connecté non terminé vers `/onboarding`, sinon `/map`. `NEXT_PUBLIC_CALCOM_URL` documentée dans `.env.example` et définie dans `.env.local`. Typecheck vert.
  </done>
</task>

<task type="auto">
  <name>Task 3: tools/verify_p7.py — preuve Playwright bout-en-bout (row DB réelle, reprise, done, Cal.com, landing)</name>
  <files>tools/verify_p7.py</files>
  <action>
Créer `tools/verify_p7.py` en MIRRORANT `tools/verify_p6.py` (mêmes helpers : `log`/`check` exit-1-au-1er-FAIL et AUCUNE preuve verte écrite sur échec ; `pg(sql)` via `docker exec -i supabase_db_skilltree-OS psql -U postgres -d postgres -tAc` ; `signup(page, base, email, pw)` qui va sur `/auth/login`, remplit email/password, clique « Créer un compte »). `BASE = os.environ.get("VERIFY_BASE", "http://localhost:3010")`. `OUT = ROOT/"orchestration"/"verify"/"p7"` (mkdir parents).

Séquence de checks (chaque check imprime PASS/FAIL + valeur mesurée) :
1. `signup` user A → `uid = pg("select id from auth.users where email=...")` non vide.
2. Row onboarding vide avant tout choix : `pg("select count(*) from onboarding where user_id='{uid}'")` == 0.
3. `page.goto(BASE + "/onboarding")` (networkidle). Cliquer un parcours : `page.click("[data-testid='onb-path-agency']")`, `wait_for_timeout(1200)` (POST fire-and-forget + upsert). Puis pg : `select path, step from onboarding where user_id` → `path='agency'` ET `step=1`. Screenshot `path-select.png` AVANT le clic (welcome), `tour-step-1.png` après.
4. Avancer jusqu'à l'étape 3 : cliquer 2× `page.click("[data-testid='onb-next']")` (par testid, indépendant du label Suivant/Terminer ; `wait_for_timeout` entre chaque). pg : `select step ...` == 3. Lire l'overlay rendu `page.locator("[data-testid='onb-step-indicator']").inner_text()` → contient « 3 » ET « 6 ». Screenshot `tour-step-3.png`.
5. ANTI-FAUX-POSITIF (reprise) : `page.goto(BASE + "/onboarding")` (reload). `wait_for_timeout(1500)` (re-hydratation). Relire `[data-testid='onb-step-indicator']` → contient « 3 » (PAS « 1 ») — la reprise vient de la DB, pas d'un state client. Ce check est la preuve centrale de ONB-01. Screenshot `resume-step-3.png`.
6. Terminer : cliquer `page.click("[data-testid='onb-next']")` jusqu'à dépasser l'étape 6 (boucle bornée, ex. 4 clics max avec garde ; le dernier clic — label « Terminer » — déclenche `complete()`), atteindre la phase reveal. pg : `select done from onboarding where user_id` → `t`. Assert CTA Cal.com : `href = page.locator("[data-testid='onb-calcom-cta']").get_attribute("href")` non vide et commence par `http`. Screenshot `reveal-calcom.png`.
7. Landing (done=true) : `page.goto(BASE + "/")` → attendre navigation → `page.url` finit par `/map`.
8. SKIP → done (user B frais) : `signup` user B, `goto /onboarding`, choisir un parcours, puis cliquer « Passer » (`page.click("text=Passer")`), `wait_for_timeout(1200)`. pg (uid B) : `select done` → `t`.
9. Landing (done=false, user C frais) : `signup` user C, NE PAS choisir de parcours, `goto /`, → `page.url` finit par `/onboarding`.

Sur tout PASS : écrire `orchestration/verify/p7/dom-assert.txt` (le rapport complet des lignes PASS) + un `row-proof.txt` récapitulant les valeurs pg observées (uid tronqué, path, step 1→3 reprise, done). Les screenshots forment la « séquence » demandée par le critère de succès #1. Sur premier FAIL : `sys.exit(1)` sans écrire de preuve verte (comportement identique à verify_p6).

Docstring en tête : prérequis (apps/web buildé via `pnpm build` SANS piper head/grep = SIGPIPE ; serveur `PERSONAL_MODE=true` sur :3010 lancé depuis un build frais, PID récupéré via `ss -ltnp`, tué par PID ; Supabase local UP en docker) et la commande de lancement `/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p7.py`.
  </action>
  <verify>
`python3 -c "import ast; ast.parse(open('tools/verify_p7.py').read())"` → parse OK (pas d'erreur de syntaxe).
`grep -n "supabase_db_skilltree-OS\|onb-step-indicator\|onb-calcom-cta\|onb-path-\|sys.exit(1)" tools/verify_p7.py` → helpers psql, testids et le fail-hard présents.
Exécution réelle (orchestrateur, après build+serveur :3010) : `/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p7.py` → « TOUS LES CHECKS PASS » et `orchestration/verify/p7/` contient dom-assert.txt + row-proof.txt + les screenshots (path-select, tour-step-1/3, resume-step-3, reveal-calcom).
  </verify>
  <done>
`tools/verify_p7.py` prouve, sur rendu et DB RÉELS : tour 1→6 avec overlay « Étape X sur 6 », parcours écrit (`path`), reprise au reload à l'étape 3 (row `step=3`, PAS un state client), skip → `done=true`, CTA Cal.com href externe, et landing post-login onboarding-aware. Aucune preuve verte écrite si un check échoue.
  </done>
</task>

</tasks>

<verification>
Après exécution des 3 tasks :
1. `cd apps/web && npx tsc --noEmit` → exit 0.
2. `cd apps/web && pnpm build` → exit 0 (LANCER SANS piper head/grep = SIGPIPE ; NEXT_PUBLIC_CALCOM_URL présent dans .env.local avant build).
3. Lancer un serveur `PERSONAL_MODE=true` sur :3010 depuis le build frais (PID via `ss -ltnp`, tuer par PID à la fin), Supabase local UP.
4. `/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p7.py` → « TOUS LES CHECKS PASS ».
5. Preuves présentes dans `orchestration/verify/p7/` (dom-assert.txt, row-proof.txt, séquence de screenshots).
</verification>

<success_criteria>
- ONB-01 : tour 6 étapes, overlay « Étape X sur 6 », reprise au reload prouvée par la row `onboarding` (step=3 après reload à l'étape 3), skip → `done=true`. [verify_p7 checks 3-8]
- ONB-02 : 4 parcours mappés à l'enum `onboarding_path` (path écrit en DB) + CTA Cal.com externe (href = NEXT_PUBLIC_CALCOM_URL). [verify_p7 checks 3, 6]
- Landing post-login onboarding-aware (done=false → /onboarding, done=true → /map). [verify_p7 checks 7, 9]
- `lib/onboarding.ts` calqué sur le pattern Phase 4 (installs.ts) ; route `/api/onboarding` et schéma NON retouchés.
- Aucune dépendance npm ajoutée ; Cal.com = simple lien externe.
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding/05-01-SUMMARY.md` (template summary) résumant : fichiers créés/évolués, décisions (mapping motive→enum, gate de landing dans app/page.tsx, Cal.com env-configurable), et le récap des preuves `orchestration/verify/p7/` (valeurs pg observées, chemins screenshots).
</output>
