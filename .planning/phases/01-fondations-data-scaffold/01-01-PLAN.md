---
phase: 01-fondations-data-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build_catalog.mjs
  - tools/assert-graph.mjs
  - apps/web/lib/catalog/types.ts
  - apps/web/lib/catalog/index.ts
  - apps/web/lib/data.ts
  - apps/web/lib/types.ts
  - apps/web/lib/lessons.ts
  - apps/web/lib/db.ts
  - apps/web/lib/map-layout.ts
  - apps/web/app/(app)/hub/page.tsx
  - apps/web/components/skill/SkillPanel.tsx
  - apps/web/components/skill/SkillCard.tsx
  - apps/web/components/map/SkillMap.tsx
  - apps/web/components/map/StageGrid.tsx
  - apps/web/components/ui/CommandBar.tsx
  - apps/web/components/tree/MyTree.tsx
  - apps/web/lib/catalog/catalog.json
  - orchestration/verify/p0/hub.png
  - orchestration/verify/p0/modules.png
  - orchestration/verify/p0/dom-assert.txt
autonomous: true

must_haves:
  truths:
    - "node tools/assert-graph.mjs imprime la ligne d'invariant exacte et sort en exit≠0 sur violation"
    - "cd apps/web && pnpm build passe au vert (prebuild régénère le catalogue + assert-graph avant next build)"
    - "/hub et /modules rendent depuis le catalogue généré, sans aucun slug inventé du prototype"
    - "apps/web/lib/data.ts n'existe plus ; tout accès catalogue passe par apps/web/lib/catalog"
  artifacts:
    - path: "apps/web/lib/catalog/catalog.json"
      provides: "Catalogue consolidé généré (source unique de vérité runtime)"
      contains: "\"jobsMap\": 137"
    - path: "apps/web/lib/catalog/types.ts"
      provides: "Types domaine émis par le build (Sector, Job/Skill, SkillFile, CommandCenter…)"
      min_lines: 30
    - path: "apps/web/lib/catalog/index.ts"
      provides: "Accesseur catalogue (SECTORS, SKILLS, CHART_JOBS, SKILL_FILES, DASHBOARDS, HUB, MODULES, helpers)"
      min_lines: 25
    - path: "tools/assert-graph.mjs"
      provides: "Invariants du graphe, exit≠0 sur écart, ligne PASS au format exact"
    - path: "orchestration/verify/p0/hub.png"
      provides: "Screenshot rendu réel de /hub"
    - path: "orchestration/verify/p0/modules.png"
      provides: "Screenshot rendu réel de /modules"
  key_links:
    - from: "apps/web/app/(app)/hub/page.tsx"
      to: "apps/web/lib/catalog"
      via: "import @/lib/catalog"
      pattern: "@/lib/catalog"
    - from: "apps/web/lib/catalog/index.ts"
      to: "apps/web/lib/catalog/catalog.json"
      via: "import catalog.json généré par build_catalog.mjs"
      pattern: "catalog/catalog\\.json"
    - from: "apps/web/package.json (prebuild)"
      to: "tools/build_catalog.mjs + tools/assert-graph.mjs"
      via: "build:catalog exécuté avant next build"
      pattern: "build_catalog\\.mjs"
---

<objective>
Verrouiller une **source de vérité catalogue unique** générée depuis `data/*.json`, et finir de rebrancher le prototype `apps/web` dessus en supprimant toute donnée catalogue hardcodée (D1, D2, D9 ; requirements DATA-01/02/03).

**État de départ réel (constaté sur disque, pas supposé)** : le commit `845ae82` a déjà fait l'essentiel de P0 — `tools/build_catalog.mjs` + `tools/assert-graph.mjs` fonctionnent (assert PASS), `apps/web/lib/catalog/catalog.json` est généré (137/78/7, chart 165/36), `package.json` a le hook `prebuild → build:catalog`, `d3-force` n'est plus une dépendance, `apps/web` est tracké git. **Ce plan est un finish + verify**, pas une reconstruction. Les écarts restants sont précis (voir tâches).

Purpose: si tout le reste échoue, la Map + le catalogue consolidé doivent fonctionner — donc la fondation data doit être asserted, immuable, et prouvée sur rendu réel.
Output: catalogue + types générés, `lib/data.ts` supprimé, pages branchées `@/lib/catalog`, preuves DOM+screenshots dans `orchestration/verify/p0/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/ARCHITECTURE.md
@docs/PLAN.md
@tools/build_catalog.mjs
@tools/assert-graph.mjs
@apps/web/lib/data.ts
@apps/web/lib/types.ts
@apps/web/app/(app)/hub/page.tsx

# Faits vérifiés au moment du plan :
# - assert-graph PASS actuel : "sectors=7 functions=34 jobs_map=137 jobs_chart=165 chart_extras=28 skills=78 dashboards=6 human_led=36 req_unresolved=0 orphans=0"
#   -> le critère de succès exige des TOKENS DIFFÉRENTS (jobs=, chart_total=, chart_human=). Tâche 1 réaligne.
# - lib/data.ts importe déjà catalog.json (n'est plus les ~20 skills inventés) mais DOIT être supprimé (D3/DATA-03).
# - 10 sites importent lib/data ("@/lib/data" OU relatif "./data") : hub/page.tsx, SkillPanel, SkillCard,
#   SkillMap, StageGrid, CommandBar, MyTree, lib/lessons.ts, lib/db.ts, lib/map-layout.ts.
#   lib/db.ts est consommé par 3 routes API (app/api/catalog, app/api/my-tree, app/api/skills/[slug]) ;
#   lib/map-layout.ts par components/map/SkillMap.tsx — les oublier casserait le build à la suppression de data.ts.
# - Slugs inventés du prototype (ancienne data.ts, à NE PLUS voir dans le DOM) : "cartographie-marche", "definition-icp", "minage-base", "scoring-leads", "brouillon-froid".
# - Vrais jobs du catalogue (English name = clé source, D9) : "Market Mapping" (slug market-mapping), "ICP Definition" (icp-definition), "Trigger Detection".
# - StageGrid/SkillMap = ancienne map (remplacée en P2) : les GARDER compilables, ne pas les embellir ici.
# - Chromium Playwright déjà installé : ~/.cache/ms-playwright/chromium-1228/ ; python playwright dispo via /home/nuveo/.higgsfield-login-venv/bin/python.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Réaligner la sortie assert-graph au format exact + émettre types.ts depuis le build</name>
  <files>tools/assert-graph.mjs, tools/build_catalog.mjs, apps/web/lib/catalog/types.ts</files>
  <action>
Deux changements ciblés, aucune régression des invariants existants (qui passent déjà).

**A. `tools/assert-graph.mjs` — ligne PASS au format exact.**
Le critère de succès #1 exige que `node tools/assert-graph.mjs` imprime **exactement** cette ligne (tokens et ordre) :

    sectors=7 functions=34 jobs=137 skills=78 chart_total=165 chart_human=36 req_unresolved=0 orphans=0

Aujourd'hui la ligne finale imprime `jobs_map=137 jobs_chart=165 chart_extras=28 ... dashboards=6 human_led=36`. Modifier UNIQUEMENT la ligne `console.log("\n[assert-graph] PASS ...")` finale pour qu'elle contienne ces 8 tokens dans cet ordre (mapper : `jobs`=jobsMap.length(137), `chart_total`=jobsChart.length(165), `chart_human`=humanLed.length(36)). Garder tous les `check(...)` internes et l'`process.exit(1)` sur `failures.length>0` inchangés (les invariants et le exit≠0 sur violation ne bougent pas). Ne pas retirer les checks de comptes — seule la ligne de résumé finale est reformattée. Il est acceptable que la ligne PASS ne ré-imprime plus `chart_extras`/`dashboards` (ils restent vérifiés par les `check()` au-dessus).

**B. `tools/build_catalog.mjs` — émettre `apps/web/lib/catalog/types.ts` (décision verrouillée : sortie = `lib/catalog/*.json` + `types.ts`).**
À la fin de la section « 6. Écriture catalogue », après le `writeFileSync(catalog.json)`, écrire aussi `apps/web/lib/catalog/types.ts` : un fichier TypeScript **généré** (bandeau en tête `// GÉNÉRÉ par tools/build_catalog.mjs — ne pas éditer à la main`) contenant les types domaine qui décrivent la forme réellement émise dans catalog.json : `SectorSlug`, `JobLevel`, `JobOrigin`, `Sector`, `JobRef`, `SkillFileRef`, `JobLadder`, `Skill` (le job), `SkillFile`, `CommandCenterStat`, `CommandCenter`, `LessonMeta`, `ModuleMeta`. Reprendre les définitions **exactes** déjà présentes dans `apps/web/lib/types.ts` (elles sont correctes et alignées sur le schéma). Le contenu peut être une string template dans le build ; le but est que le build POSSÈDE et régénère ce fichier. Log une ligne `[build_catalog] écrit -> apps/web/lib/catalog/types.ts`.

Ne PAS toucher à la logique de réconciliation (137/28/165/36), ni aux comptes, ni au SECTOR_FR.
  </action>
  <verify>
`node tools/build_catalog.mjs` → imprime les 2 lignes "écrit ->" (catalog.json + types.ts) ; `test -f apps/web/lib/catalog/types.ts`.
`node tools/assert-graph.mjs | tail -1` → la dernière ligne est EXACTEMENT : `[assert-graph] PASS sectors=7 functions=34 jobs=137 skills=78 chart_total=165 chart_human=36 req_unresolved=0 orphans=0` (le préfixe `[assert-graph] PASS ` est toléré ; les 8 tokens `clé=valeur` doivent apparaître dans cet ordre). Extraire et comparer avec : `node tools/assert-graph.mjs | grep -oE 'sectors=7 functions=34 jobs=137 skills=78 chart_total=165 chart_human=36 req_unresolved=0 orphans=0'` doit renvoyer la chaîne (exit 0 du grep).
Test exit≠0 : dupliquer temporairement une entrée pour casser un compte (ou `EXPECT.skills=79`), relancer `node tools/assert-graph.mjs; echo "exit=$?"` → doit imprimer `exit=1` et lister l'invariant violé ; **restaurer** ensuite l'état.
  </verify>
  <done>La ligne PASS matche exactement les 8 tokens requis ; `types.ts` est généré par le build ; toute violation d'invariant fait exit=1.</done>
</task>

<task type="auto">
  <name>Task 2: Créer l'accesseur lib/catalog, rebrancher les 10 imports, supprimer lib/data.ts</name>
  <files>apps/web/lib/catalog/index.ts, apps/web/lib/data.ts, apps/web/lib/types.ts, apps/web/lib/lessons.ts, apps/web/lib/db.ts, apps/web/lib/map-layout.ts, apps/web/app/(app)/hub/page.tsx, apps/web/components/skill/SkillPanel.tsx, apps/web/components/skill/SkillCard.tsx, apps/web/components/map/SkillMap.tsx, apps/web/components/map/StageGrid.tsx, apps/web/components/ui/CommandBar.tsx, apps/web/components/tree/MyTree.tsx</files>
  <action>
Objectif DATA-03 : `lib/data.ts` supprimé, tout accès catalogue centralisé dans `lib/catalog`, plus aucune donnée catalogue hardcodée.

1. **Créer `apps/web/lib/catalog/index.ts`** = déplacer intégralement le contenu de `apps/web/lib/data.ts` ici (mêmes exports, même comportement) : `import catalog from "./catalog.json"` (chemin relatif devient `./catalog.json`), types importés depuis `./types` (le types.ts généré par Task 1) OU `../types` selon ce qui compile ; réexporter `SECTORS`, `SKILLS` (filtre `origin==='map'`), `CHART_JOBS`, `SKILL_FILES`, `DASHBOARDS`, `HUB`, `MODULES`, `FRESH_DROPS=[]`, `MOST_INSTALLED=[]`, et les helpers `bySector`, `sectorOf`, `skillBySlug`. Garder le bandeau « généré, ne pas éditer catalog.json à la main ». `MODULES` (3 métadonnées de modules) reste ici (ce n'est pas de la donnée catalogue au sens data/*.json ; c'est de la config d'app — acceptable, non hardcodé-catalogue).

2. **`apps/web/lib/types.ts`** : le transformer en ré-export du types généré → `export * from "./catalog/types";` + garder UNIQUEMENT les types propres à l'app absents du catalogue s'il y en a (vérifier : `LessonMeta`, `ModuleMeta` sont déjà dans le types généré par Task 1 → si oui, ne rien redéfinir). But : un seul lieu de définition, pas de doublon divergent. Tous les `import ... from "@/lib/types"` existants doivent continuer à résoudre.

3. **Rebrancher les 10 sites** de `@/lib/data` (ou relatif `./data`) → `@/lib/catalog` (ou `./catalog`) — remplacement d'import pur, mêmes symboles :
   - `apps/web/app/(app)/hub/page.tsx` (FRESH_DROPS, MOST_INSTALLED, MODULES)
   - `apps/web/components/skill/SkillPanel.tsx` (sectorOf)
   - `apps/web/components/skill/SkillCard.tsx` (sectorOf)
   - `apps/web/components/map/SkillMap.tsx` (skillBySlug)
   - `apps/web/components/map/StageGrid.tsx` (SKILLS, SECTORS, sectorOf)
   - `apps/web/components/ui/CommandBar.tsx` (SKILLS)
   - `apps/web/components/tree/MyTree.tsx` (SKILLS)
   - `apps/web/lib/lessons.ts` (`import { MODULES } from "./data"` → `from "./catalog"`)
   - `apps/web/lib/db.ts` (`import { SECTORS, SKILLS, skillBySlug } from "./data"` → `from "./catalog"`) — attention : consommé par les 3 routes API `app/api/catalog/route.ts`, `app/api/my-tree/route.ts`, `app/api/skills/[slug]/route.ts` ; vérifier que ces routes compilent toujours après le rewire
   - `apps/web/lib/map-layout.ts` (`import { SECTORS, SKILLS } from "./data"` → `from "./catalog"`) — consommé par `components/map/SkillMap.tsx`

4. **Dé-hardcoder le header de `/hub`** : la `<h1>` contient « 13 skills en ligne … » (nombre catalogue en dur = violation DATA-03). Remplacer « 13 » par un compte dérivé du catalogue, p.ex. `SKILL_FILES.length` (78 skills installables) — importer depuis `@/lib/catalog`. Formule : ``{SKILL_FILES.length} skills en ligne · nouveaux drops cette semaine · reprends où tu t'es arrêté``. Garde le reste du texte FR intact.

5. **Supprimer `apps/web/lib/data.ts`** (`git rm apps/web/lib/data.ts` ou suppression fichier).

Ne PAS refactorer StageGrid/SkillMap au-delà de l'import (map refaite en P2). Ne pas casser le build : si un symbole manque après suppression, l'ajouter à `lib/catalog/index.ts`, jamais recréer data.ts.
  </action>
  <verify>
`grep -rn "@/lib/data\b\|from \"\\./data\"\|from '\\./data'" apps/web/app apps/web/components apps/web/lib` → **0 résultat**.
`test ! -f apps/web/lib/data.ts && echo "data.ts supprimé"`.
`cd apps/web && pnpm typecheck` → 0 erreur TypeScript.
`grep -c "13 skills en ligne" apps/web/app/\(app\)/hub/page.tsx` → 0 (compte dynamique en place).
  </verify>
  <done>lib/data.ts n'existe plus ; aucun import résiduel `@/lib/data` ; typecheck vert ; header /hub dérivé du catalogue.</done>
</task>

<task type="auto">
  <name>Task 3: Preuve sur rendu réel — build vert + DOM assert + screenshots /hub et /modules</name>
  <files>apps/web/lib/catalog/catalog.json, orchestration/verify/p0/hub.png, orchestration/verify/p0/modules.png, orchestration/verify/p0/dom-assert.txt</files>
  <action>
Anti-faux-positif (règle globale) : ne pas conclure sur un exit 0. Prouver le rendu réel.

1. **Build complet** : `cd apps/web && pnpm build` — le hook `prebuild` doit relancer `build:catalog` (build_catalog + assert-graph, avec la ligne exacte de Task 1) PUIS `next build`. Doit finir vert. Capturer la sortie ; vérifier que la ligne assert-graph exacte apparaît dans le log de build.

2. **Commit du catalogue généré** : s'assurer que `apps/web/lib/catalog/catalog.json` et `apps/web/lib/catalog/types.ts` sont **trackés git** (actuellement `catalog.json` est untracked). Raison : `next dev` NE lance PAS `prebuild` — le fichier doit exister pour que le dev server et l'import compilent sur un checkout frais. `git add apps/web/lib/catalog/catalog.json apps/web/lib/catalog/types.ts`.

3. **Dev server + DOM assert** : lancer `cd apps/web && pnpm build && pnpm start` (ou `pnpm dev`) en arrière-plan sur un port libre (ex. 3010), attendre l'écoute réelle (poll `curl -sf http://localhost:3010/hub` jusqu'à succès, timeout 60s — ne pas conclure avant écoute effective). Puis, en écrivant les résultats dans `orchestration/verify/p0/dom-assert.txt` :
   - `curl -s http://localhost:3010/hub` → HTML : DOIT contenir un contenu dérivé catalogue (`78 skills en ligne` ET un titre de module réel « Commence ici » / « Fondations Claude Code ») ; DOIT NE PAS contenir les slugs inventés du prototype : `grep -Ei "cartographie-marche|definition-icp|minage-base|scoring-leads|brouillon-froid"` → **0 match**.
   - `curl -s http://localhost:3010/modules` → HTML : HTTP 200, contient les titres de modules réels (« Commence ici », etc.) ; 0 slug inventé (même grep).
   - **Preuve job réel** (catalogue niveau job) : `curl -s http://localhost:3010/map` → doit contenir un vrai nom de job anglais du catalogue, ex. `Market Mapping` (ou `ICP Definition`) rendu par StageGrid/CommandBar depuis `@/lib/catalog`. Logguer le nom trouvé.
   Écrire dans dom-assert.txt : pour chaque page, la liste des assertions et PASS/FAIL, + la ligne assert-graph exacte, + le nom de job réel trouvé.

4. **Screenshots** : capturer `/hub` et `/modules` en PNG dans `orchestration/verify/p0/`. Méthode recommandée (browsers déjà installés) — petit script python via `/home/nuveo/.higgsfield-login-venv/bin/python` :
   ```python
   from playwright.sync_api import sync_playwright
   with sync_playwright() as p:
       b = p.chromium.launch()
       pg = b.new_page(viewport={"width":1440,"height":900})
       for path,out in [("/hub","hub.png"),("/modules","modules.png")]:
           pg.goto(f"http://localhost:3010{path}", wait_until="networkidle")
           pg.screenshot(path=f"orchestration/verify/p0/{out}", full_page=True)
       b.close()
   ```
   Fallback si playwright python indisponible : chromium headless direct — `~/.cache/ms-playwright/chromium-1228/chrome-linux/chrome --headless=new --hide-scrollbars --window-size=1440,900 --screenshot=orchestration/verify/p0/hub.png http://localhost:3010/hub`.
   Arrêter le serveur en fin de tâche.
  </action>
  <verify>
`cd apps/web && pnpm build` exit 0 et log contient `jobs=137 skills=78 chart_total=165 chart_human=36`.
`git ls-files apps/web/lib/catalog/catalog.json apps/web/lib/catalog/types.ts` → les 2 listés (trackés).
`orchestration/verify/p0/dom-assert.txt` : toutes les assertions marquées PASS ; 0 slug inventé sur /hub et /modules ; un vrai nom de job (`Market Mapping` ou équivalent) trouvé sur /map.
`test -s orchestration/verify/p0/hub.png && test -s orchestration/verify/p0/modules.png` (fichiers non vides, >5 Ko chacun : `[ $(stat -c%s orchestration/verify/p0/hub.png) -gt 5000 ]`).
  </verify>
  <done>`pnpm build` vert ; /hub et /modules rendent depuis le catalogue sans slug inventé ; job réel prouvé ; 2 screenshots non vides dans orchestration/verify/p0/ ; catalogue généré tracké git.</done>
</task>

</tasks>

<verification>
Critères de succès de la phase (observables, anti-faux-positif — pas « build vert » seul) :

1. **Invariants** : `node tools/assert-graph.mjs` imprime exactement
   `sectors=7 functions=34 jobs=137 skills=78 chart_total=165 chart_human=36 req_unresolved=0 orphans=0`
   et sort en **exit≠0** sur tout invariant violé (test de régression : casser un compte → exit=1, puis restaurer).
2. **Rendu réel** : `cd apps/web && pnpm build` vert ; `/hub` et `/modules` servis par le dev/prod server rendent depuis `apps/web/lib/catalog/` — DOM assert : contenu catalogue réel présent (`78 skills`, titres de modules réels ; nom de job réel « Market Mapping » sur /map), **aucun** slug inventé du prototype (`cartographie-marche`, `definition-icp`, …).
3. **Nettoyage + preuves** : `apps/web/lib/data.ts` supprimé, `grep -r "@/lib/data" apps/web` = 0 ; screenshots `/hub` et `/modules` dans `orchestration/verify/p0/` (non vides) ; `catalog.json` + `types.ts` générés et trackés git.
</verification>

<success_criteria>
- `node tools/assert-graph.mjs` : ligne d'invariant exacte + exit≠0 sur violation ✓
- `pnpm build` vert avec prebuild→build:catalog→assert-graph câblé ✓
- `/hub` + `/modules` rendent depuis `lib/catalog`, 0 slug inventé, job réel prouvé ✓
- `lib/data.ts` supprimé, 0 import résiduel, `pnpm typecheck` vert ✓
- `lib/catalog/types.ts` généré par le build ; `lib/catalog/index.ts` = accesseur unique ✓
- Preuves DOM + 2 screenshots dans `orchestration/verify/p0/` ✓
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondations-data-scaffold/01-01-SUMMARY.md`.
</output>
