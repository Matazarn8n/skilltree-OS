---
phase: 02-hero-constellation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/components/constellation/JobPanel.tsx
  - apps/web/components/constellation/ConstellationWheel.tsx
  - apps/web/components/constellation/SectorView.tsx
  - apps/web/components/constellation/JobNode.tsx
  - apps/web/components/map/MapView.tsx
  - apps/web/components/skill/SkillPanel.tsx
  - apps/web/app/globals.css
  - tools/verify_p1.py
  - orchestration/verify/p1/dom-assert.txt
  - orchestration/verify/p1/map.png
  - orchestration/verify/p1/sector-zoom.png
  - orchestration/verify/p1/panel.png
  - orchestration/verify/p1/lowfx-390.png
autonomous: true

must_haves:
  truths:
    - "Enter/clic sur un nœud job ouvre un JobPanel complet : eyebrow niveau, titre, breadcrumb secteur·fonction, desc, carte fichier skill, BREAKS INTO (chips), BUILDS ON cliquable, WHAT IT REPLACES, LADDER 3 niveaux, THE HUMAN"
    - "Cliquer un prérequis dans BUILDS ON ouvre le panel de CE job (navigation intra-panel)"
    - "Tab parcourt secteur→job sur les 137 nœuds, chaque aria-label non vide ; Échap ferme le panel et rend le focus au nœud d'origine"
    - "Avec prefers-reduced-motion émulé : 0 animation (computed style animation/transition ≈ 0) ; viewport 390px : data-lowfx=true"
    - "Contraste AA prouvé par mesure : ratio de luminance ≥ 4.5:1 cité pour label job et tagline secteur"
    - "Preuves écrites dans orchestration/verify/p1/ : DOM asserts cités + 4 screenshots réels"
  artifacts:
    - path: "apps/web/components/constellation/JobPanel.tsx"
      provides: "panel job latéral complet, sections FR, gère champs null"
      min_lines: 120
    - path: "tools/verify_p1.py"
      provides: "suite Playwright reproductible : asserts DOM + reduced-motion + LOWFX + screenshots"
      min_lines: 80
    - path: "orchestration/verify/p1/dom-assert.txt"
      provides: "preuve citée : 137 focusables, aria-labels, Tab→Enter→panel, reduced-motion, 390px, ?view="
  key_links:
    - from: "apps/web/components/constellation/JobPanel.tsx"
      to: "apps/web/lib/catalog"
      via: "skillBySlug pour la navigation BUILDS ON"
      pattern: "skillBySlug"
    - from: "apps/web/components/map/MapView.tsx"
      to: "JobPanel"
      via: "selected → <JobPanel job={selected}> (SkillPanel provisoire retiré)"
      pattern: "JobPanel"
    - from: "tools/verify_p1.py"
      to: "http://localhost:3010/map"
      via: "Playwright chromium (reduced_motion='reduce', viewport 390)"
      pattern: "reduced_motion|localhost:3010"
---

<objective>
Livrer le JobPanel complet fidèle aux captures `map_panel/`, durcir l'a11y clavier et LOWFX/reduced-motion, puis prouver les 4 critères de phase sur rendu réel avec une suite Playwright reproductible écrite dans `orchestration/verify/p1/`.

Purpose: MAP-02 (panel complet), MAP-03 (clavier + AA), MAP-04 (reduced-motion + LOWFX) — et clôture prouvée de la Phase 2.
Output: `JobPanel.tsx` branché sur la roue, `tools/verify_p1.py`, preuves `orchestration/verify/p1/` (dom-assert.txt + map.png + sector-zoom.png + panel.png + lowfx-390.png).
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-constellation/02-01-SUMMARY.md   (décisions géométrie/composants du plan 1)
@docs/07_VISUAL_SPEC.md          (§Vue MAP, alinéa « Panel job » — structure exacte observée)
@apps/web/lib/catalog/types.ts   (Skill : ladder {manual,assisted,autonomous}, replaces, human, requires[], files[], skills[])
@apps/web/lib/catalog/index.ts   (skillBySlug, sectorOf, HUB)
@apps/web/components/skill/SkillPanel.tsx   (panel provisoire à remplacer — reprendre Échap/backdrop, pas le contenu)

Références visuelles (regarder avant de coder) :
- captures/map_panel/sales__icp-definition_status_live.png (+ _dev, _notstarted)
- captures/map_panel/deals__objection-response_top.png et _scrolled.png (structure complète du scroll)
</context>

<constraints>
- **Chrome UI réécrit FR, contenu catalogue tel quel** : les libellés de sections sont des réécritures FR (jamais la prose du produit copiée) ; `desc`, `ladder.*`, `replaces`, `human` viennent du catalogue (texte source anglais — la traduction de la donnée n'est PAS dans cette phase). Structure des sections = identique à l'original (contrat de fidélité).
- Champs nullables (`ladder`, `replaces`, `human`, `stage` peuvent être null ; `files`/`skills`/`requires` peuvent être vides) → section omise proprement, jamais « undefined » rendu.
- Contraste AA : textes sur `--bg-panel` en `--text` / `--text-muted` (ratios déjà ≥ AA) ; ne pas introduire de texte < 11px en `--text-faint` pour du contenu porteur de sens.
- Anti-faux-positif : chaque assert de `dom-assert.txt` cite la commande/le code et la sortie réelle. Jamais `|| true`.
- Serveur de preuve : `pnpm build` puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python` (playwright py installé, chromium dans `~/.cache/ms-playwright`) ; si ce venv échoue, fallback documenté : `npm i -D playwright` dans apps/web + script node équivalent.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: JobPanel complet FR branché sur la roue</name>
  <files>apps/web/components/constellation/JobPanel.tsx, apps/web/components/map/MapView.tsx, apps/web/components/skill/SkillPanel.tsx (suppression si plus consommé)</files>
  <action>
Regarder `captures/map_panel/deals__objection-response_top.png` + `_scrolled.png` + les 3 variantes status avant de coder.

Créer `apps/web/components/constellation/JobPanel.tsx` — panneau latéral droit sombre (`role="dialog" aria-modal aria-label={job.name}`, backdrop clic = fermer, Échap = fermer — reprendre le squelette du SkillPanel existant), scrollable, avec dans l'ordre :

1. **Eyebrow niveau** (uppercase tracking large, couleur secteur) : `autonomous` → « Pleinement autonome », `assisted` → « Assisté par l'humain », `human-led` → « Piloté par l'humain ».
2. **Titre display serif** = `job.name` (nom source, D9) + badge outline « Commence ici » si `job.notes` commence par « Start here » (seul signal disponible ; sinon pas de badge).
3. **Breadcrumb** : `{sector.name FR} · {job.function}`.
4. **Description** : `job.desc`.
5. **Carte CTA fichier skill** (si `files.length > 0`) : « {n} fichier(s) skill exécutable(s) · à toi » + bouton « Récupère-le ↓ » (disabled, title « Disponible avec le state user — Phase 4 » — pas de faux download).
6. **« SE DÉCOMPOSE EN »** (BREAKS INTO) : chips des `job.skills` (slugs de capacité, rendu monospace/chip).
7. **« S'APPUIE SUR »** (BUILDS ON) : chips **cliquables** des `job.requires` (`{slug, name}`) → `onNavigate(slug)` : le parent résout `skillBySlug(slug)` et remplace le job affiché (le panel reste ouvert). Si le prérequis est le hub (`slug === "company-knowledge-base"` / `HUB.slug`), chip avec icône maison NON cliquable (le hub n'est pas un job).
8. **« CE QUE ÇA REMPLACE »** (WHAT IT REPLACES) : encadré citation avec `job.replaces` (omis si null).
9. **« L'ÉCHELLE »** (THE LADDER) : 3 lignes fixes — « Piloté humain » / `ladder.manual`, « Assisté » / `ladder.assisted`, « Autonome » / `ladder.autonomous` — avec la ligne correspondant à `job.level` mise en évidence (couleur secteur). Omis si `ladder` null.
10. **« L'HUMAIN »** (THE HUMAN) : paragraphe `job.human` (omis si null).
11. Bouton fermer ✕ en haut à droite.

Focus management : à l'ouverture, focus sur le bouton ✕ ; mémoriser `document.activeElement` avant ouverture et le re-focus à la fermeture (Échap, ✕ ou backdrop) — nécessaire pour le critère clavier.

**MapView.tsx** : remplacer le `SkillPanel` provisoire par `JobPanel` ; ajouter `onNavigate` (met à jour `selected` via `skillBySlug`). Puis `grep -rn "SkillPanel" apps/web --include=*.tsx` — s'il n'est plus consommé nulle part, `git rm apps/web/components/skill/SkillPanel.tsx` (garder `AutonomyBadge`/`SkillCard` s'ils sont utilisés ailleurs, ex. hub).
  </action>
  <verify>`pnpm typecheck` + `pnpm build` verts ; en dev (`pnpm dev`), ouvrir /map, cliquer un nœud → les 10 blocs apparaissent pour un job complet (ex. icp-definition) et aucun « undefined » pour un job à champs null (prendre un job `human-led`).</verify>
  <done>Panel complet conforme à la structure des captures, navigation BUILDS ON intra-panel fonctionnelle, panel provisoire retiré.</done>
</task>

<task type="auto">
  <name>Task 2: A11y clavier + LOWFX/reduced-motion durcis</name>
  <files>apps/web/components/constellation/ConstellationWheel.tsx, apps/web/components/constellation/SectorView.tsx, apps/web/components/constellation/JobNode.tsx, apps/web/app/globals.css</files>
  <action>
1. **Ordre de tabulation** : vérifier que le DOM rend, pour chaque secteur dans l'ordre `order` : bouton secteur PUIS ses jobs (fonction par fonction, rayon croissant) — c'est l'ordre du layout du plan 02-01 ; corriger si un wrapper casse l'ordre. Aucun `tabIndex` positif.
2. **Aria** : bouton secteur `aria-label="{sector.name} — {n} jobs"` ; chaque JobNode `aria-label` non vide (déjà posé au plan 1 — vérifier) ; conteneur roue `role="group" aria-label="Constellation des 137 jobs"`. Le switcher garde `aria-current`.
3. **Enter/Espace** : natifs sur `<button>` — s'assurer qu'aucun `preventDefault` ne les casse ; Échap dans SectorView (hors panel) → retour all-departments.
4. **Reduced-motion** : `globals.css` a déjà `@media (prefers-reduced-motion: reduce)` qui force animation/transition à 0.001ms sur `*` — étendre pour couvrir aussi `scroll-behavior: auto !important` et vérifier qu'AUCUNE animation JS n'existe (grep `requestAnimationFrame|setInterval` dans components/constellation → doit être vide ; la roue du plan 1 n'en a pas, garder ça).
5. **LOWFX <700px** : quand `data-lowfx="true"` : particules réduites (24), transitions CSS non montées (classe conditionnelle), pas d'ombres/filtres SVG. Ajouter dans globals.css un garde `[data-lowfx="true"] * { transition-duration: 0.001ms !important; animation-duration: 0.001ms !important; }`.
6. **Contraste** : passer en revue les textes de la roue (labels jobs, taglines) — porteurs de sens ≥ ratio AA sur `--bg` (utiliser `--text-muted` #9aa3c4 min, pas `--text-faint` pour les noms de jobs).
  </action>
  <verify>`pnpm build` vert ; `grep -rn "requestAnimationFrame\|setInterval" apps/web/components/constellation apps/web/lib/constellation` → 0 résultat ; en dev avec DevTools émulation reduced-motion, hover d'un nœud = aucun mouvement.</verify>
  <done>Parcours clavier complet secteur→job, Échap contextuel, 0 animation JS, gardes CSS reduced-motion + LOWFX en place.</done>
</task>

<task type="auto">
  <name>Task 3: Suite de preuves Playwright — orchestration/verify/p1/</name>
  <files>tools/verify_p1.py, orchestration/verify/p1/dom-assert.txt, orchestration/verify/p1/map.png, orchestration/verify/p1/sector-zoom.png, orchestration/verify/p1/panel.png, orchestration/verify/p1/lowfx-390.png</files>
  <action>
Écrire `tools/verify_p1.py` (Playwright sync API, exécuté avec `/home/nuveo/.higgsfield-login-venv/bin/python`) — chaque check imprime PASS/FAIL avec la valeur mesurée, exit 1 au premier FAIL. Préalable : `pnpm build` puis `npx next start -p 3010 &` + poll UP (le script peut supposer le serveur lancé ; documenter la commande dans dom-assert.txt).

Checks (viewport desktop 1440×900 sauf mention) :
1. **137 focusables** : `page.locator('[data-node]').count() == 137` ; chaque élément est un `<button>` (`evaluate: el.tagName`) ; `aria-label` non vide pour les 137 (boucle, compter les vides = 0).
2. **Couleurs secteur** : pour les 7 secteurs, le bouton/hub secteur porte la couleur exacte — comparer le style calculé ou l'attribut au hex du catalogue converti rgb (ex. sales `#FF9D5C` → `rgb(255, 157, 92)`).
3. **Tab secteur→job** : focus sur le premier bouton secteur (`page.keyboard.press("Tab")` en boucle depuis body OU `locator.focus()` sur le 1er secteur puis Tabs) ; asserter que la séquence de `document.activeElement.getAttribute('aria-label')` rencontre d'abord un label secteur puis des labels jobs du MÊME secteur.
4. **Enter → JobPanel réel** : focus sur le nœud `icp-definition` (`[aria-label^="ICP Definition"]`), `Enter` → `role=dialog` visible ; asserter la présence textuelle de : (a) **l'eyebrow niveau** — `icp-definition.level === "autonomous"` → « Pleinement autonome » présent dans le dialog ; (b) **la carte CTA fichier skill** — `icp-definition.files.length === 1` (ICP Strategist) → carte rendue : texte « 1 fichier skill exécutable » (ou le libellé FR exact posé en Task 1, cité tel quel dans dom-assert.txt) + bouton « Récupère-le ↓ » présent ; (c) les sections « Se décompose en », « S'appuie sur », « Ce que ça remplace », « L'échelle » (et ses 3 lignes Piloté humain/Assisté/Autonome), « L'humain », + `desc` non vide. Screenshot `panel.png`.
5. **BUILDS ON cliquable** : job épinglé = **`market-mapping`** (son `requires` = `[{slug:"icp-definition"}]`, un vrai job — NE PAS utiliser `icp-definition`, dont l'unique prérequis est le hub, chip non cliquable par design). Ouvrir le panel de `market-mapping` (`[aria-label^="Market Mapping"]` + Enter), cliquer la chip « ICP Definition » dans « S'appuie sur » → asserter que le titre du dialog devient « ICP Definition » (navigation intra-panel).
5b. **Régression champs null** : ouvrir le panel du job épinglé **`sop-generation`** (human-led, `human === null`) → asserter que l'`innerText` complet du dialog ne contient ni « undefined » ni « null », et que la section « L'humain » est absente.
5c. **Contraste AA (MAP-03)** : mesure calculée, pas déclarative — sur (i) le label texte d'un JobNode et (ii) une tagline de secteur : lire `getComputedStyle(el).color`, remonter les ancêtres jusqu'à la première `backgroundColor` non transparente (fallback : `--bg` #060810 du body), calculer le ratio de luminance relative WCAG et asserter **≥ 4.5:1** (helper ~15 lignes dans verify_p1.py ; citer les deux ratios mesurés dans dom-assert.txt). Fallback UNIQUEMENT si la mesure est impraticable (fond en gradient non résoluble) : documenter dans dom-assert.txt les paires de tokens exactes utilisées (ex. `--text-muted` #9aa3c4 sur `--bg` #060810 ≈ 7.0:1) avec leur ratio calculé hors-browser — jamais un simple « AA OK » non chiffré.
6. **Reduced-motion** : nouveau context `reduced_motion="reduce"` → sur un `[data-node]` et sur le conteneur roue : `getComputedStyle(el).animationDuration` et `transitionDuration` chacun ≤ 0.001s (parser la valeur ; « 0s » accepté) — 0 animation.
7. **LOWFX 390px** : context viewport 390×844 → `[data-lowfx="true"]` présent. Screenshot `lowfx-390.png`.
8. **?view= shareable** : `page.goto('/map?view=chart')` en navigation directe → pill CHART `aria-current` + placeholder visible ; idem `?view=dashboards`. (URL directe = shareable, pas de state client requis.)
9. **Zoom secteur** : cliquer le hub Sales → labels de fonction visibles (« Targeting »… du catalogue) + bouton retour ; screenshot `sector-zoom.png` ; retour → roue complète ; screenshot `map.png` (roue all-departments).

Écrire `orchestration/verify/p1/dom-assert.txt` : date, commandes serveur, puis pour chaque check : description, mesure citée (comptes réels, valeurs computed style), PASS/FAIL. Comparer à l'œil `map.png` vs `captures/01_after_login.png` et `sector-zoom.png` vs `captures/map_zoom/sales.png` ; noter le verdict fidélité (7 fans, hub, couleurs) dans le fichier. Tuer le serveur à la fin.

Si un check FAIL → corriger le composant, relancer, ne consigner que des preuves vertes réelles (jamais éditer le txt à la main pour verdir).
  </action>
  <verify>`/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p1.py` (serveur :3010 up) → tous checks PASS, exit 0 ; les 4 PNG existent et font >30 Ko chacun ; dom-assert.txt contient « 137 » et les valeurs computed style citées.</verify>
  <done>Les 4 success criteria de la Phase 2 sont prouvés sur rendu réel, preuves reproductibles dans orchestration/verify/p1/ via un script rejouable.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert (prebuild assert-graph inclus).
- `tools/verify_p1.py` exit 0 : 137 boutons aria-labelisés, couleurs 7/7, Tab secteur→job, Enter→panel complet sur `icp-definition` (eyebrow « Pleinement autonome » + carte fichier skill + sections structurelles), BUILDS ON navigue (`market-mapping` → chip « ICP Definition » → panel icp-definition), régression champs null (`sop-generation` : 0 « undefined »), contraste AA mesuré ≥ 4.5:1 (2 ratios cités), reduced-motion = 0 animation (computed style cité), 390px → data-lowfx, ?view= × 2 en URL directe.
- `orchestration/verify/p1/` : dom-assert.txt + map.png + sector-zoom.png + panel.png + lowfx-390.png (+ wheel-smoke.txt/wheel.png du plan 01).
- Verdict fidélité visuelle vs `captures/01_after_login.png` et `captures/map_zoom/sales.png` consigné.
</verification>

<success_criteria>
1. Screenshot `/map` vs `captures/` : 7 fans, hub central, couleurs secteur exactes (Criterion 1 de phase).
2. 137 nœuds focusables aria-labelisés ; Tab secteur→job ; Enter ouvre le JobPanel d'un job réel avec level, desc, BREAKS INTO, BUILDS ON cliquable, WHAT IT REPLACES, LADDER 3 niveaux, THE HUMAN, fichier skill (Criterion 2).
3. prefers-reduced-motion émulé → 0 animation asserté par computed style ; viewport 390px → LOWFX actif (Criterion 3).
4. Switcher `?view=` shareable avec placeholders (Criterion 4). Preuves dans `orchestration/verify/p1/`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-constellation/02-02-SUMMARY.md`
</output>
