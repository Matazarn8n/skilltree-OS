---
phase: 03-vues-modules-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/tree/page.tsx
  - apps/web/app/(app)/community/page.tsx
  - apps/web/app/(app)/settings/page.tsx
  - apps/web/components/tree/MyTree.tsx
  - apps/web/components/tree/TreeAudit.tsx
  - apps/web/components/tree/WeeklyDrops.tsx
  - apps/web/components/settings/SettingsSection.tsx
  - tools/verify_p5.py
  - orchestration/verify/p5/dom-assert.txt
  - orchestration/verify/p5/tree.png
  - orchestration/verify/p5/community.png
  - orchestration/verify/p5/settings.png

autonomous: true

# NOTE modèle/effort (GSD) : sonnet-4-6 / medium. Revue fable/opus avant intégration.
#
# DÉPENDANCE PARTAGÉE DÉCLARÉE (unique du fan-out) : le TreeAudit doit changer quand un
# skill est installé. L'état install est écrit par le plan 03-01 (Hub) via lib/installs.ts
# (localStorage `skilltree.installs.v1` + event `skilltree:installs`). CE plan LIT le MÊME
# contrat localStorage via un helper LOCAL (`useInstalledSlugs()` dans MyTree/TreeAudit) —
# il n'IMPORTE PAS lib/installs.ts (fichier de 03-01) et ne le modifie pas. Couplage =
# contrat de clé localStorage, pas de code partagé → wave:1, depends_on:[], sans conflit
# de fichier. La preuve verify_p5.py SÈME l'install en écrivant la clé localStorage +
# dispatch event directement (page.evaluate), donc 03-04 est vérifiable en isolation même
# sans 03-01. Phase 4 : les deux liront /api/me (installs) — le contrat de clé disparaît.

must_haves:
  truths:
    - "/tree rend fidèle à captures/nav_tree.png : ConnectMachine (commande git + copier), rangée de stats, Weekly drops, TreeAudit « ton arbre, calculé »"
    - "Le compteur du TreeAudit (skills installés / stats) AUGMENTE quand un skill est installé : écrire la clé localStorage `skilltree.installs.v1` + dispatch `skilltree:installs` fait passer le compteur de N à N+1 sans reload"
    - "/community rend fidèle à captures/nav_community.png : bandeau « 100 premiers », composer, feed de posts"
    - "/settings rend fidèle à captures/nav_settings.png : compte + email, plan « Membre · 47 $/mois » affiché, sécurité (mot de passe), facturation (placeholder Stripe), logout"
    - "Preuves dans orchestration/verify/p5/ : dom-assert.txt cité (compteur TreeAudit avant/après install, Member 47$) + tree.png + community.png + settings.png"
  artifacts:
    - path: "apps/web/components/tree/TreeAudit.tsx"
      provides: "audit dérivé des installs locaux : lit le contrat localStorage `skilltree.installs.v1` (helper local, réactif à l'event), liste les skills installés + compteur"
      min_lines: 40
    - path: "apps/web/components/tree/MyTree.tsx"
      provides: "vue My Tree complète : ConnectMachine + stats + WeeklyDrops + TreeAudit, compteur réactif aux installs"
      min_lines: 60
    - path: "tools/verify_p5.py"
      provides: "Playwright : 3 screenshots vs captures, TreeAudit compteur avant/après install (seed localStorage), Member 47$ présent"
      min_lines: 60
    - path: "orchestration/verify/p5/dom-assert.txt"
      provides: "preuve citée : compteur TreeAudit N→N+1, « Membre · 47 $/mois » présent, headings des 3 vues"
  key_links:
    - from: "apps/web/components/tree/TreeAudit.tsx"
      to: "localStorage `skilltree.installs.v1` (contrat de 03-01, lu via helper local)"
      via: "helper useInstalledSlugs() : read + addEventListener('skilltree:installs')"
      pattern: "skilltree.installs.v1|skilltree:installs"
    - from: "apps/web/app/(app)/settings/page.tsx"
      to: "libellé plan"
      via: "« Membre · 47 $/mois » rendu dans la section Compte"
      pattern: "47"
---

<objective>
Livrer les 3 vues restantes fidèles aux captures : **My Tree** (`nav_tree.png` — ConnectMachine, stats, Weekly drops, TreeAudit calculé depuis les installs locaux), **Community** (`nav_community.png` — bandeau « 100 premiers », feed), **Settings** (`nav_settings.png` — compte, Member $47/mo, billing placeholder, logout). Le TreeAudit réagit aux installs (compteur avant/après). Preuves sur rendu réel dans `orchestration/verify/p5/`.

Purpose: TREE-01 (My Tree + TreeAudit réactif), COMM-01 (Community), SET-01 (Settings) — P5 docs/PLAN.md.
Output: 3 pages + composants Tree, `tools/verify_p5.py`, preuves `orchestration/verify/p5/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md            (§3.2 My Tree/Community/Settings, D8 Stripe-ready)
@docs/07_VISUAL_SPEC.md          (§Community/Settings/Tree — références PNG)
@apps/web/components/tree/MyTree.tsx      (implémentation actuelle : ConnectMachine + stats + audit dérivé de SKILLS.autonomous — À REBRANCHER sur les installs locaux)
@apps/web/lib/progress.ts        (PATTERN localStorage + event à cloner pour le helper useInstalledSlugs local)
@apps/web/components/community/CommunityFeed.tsx  (feed existant — réutiliser)
@apps/web/app/(app)/settings/page.tsx    (Settings existant : « Membre · 47 $/mois » déjà présent — vérifier fidélité)

Références visuelles (regarder AVANT de coder) :
- captures/nav_tree.png, captures/nav_community.png, captures/nav_settings.png
- captures/content/nav_tree.json, nav_community.json, nav_settings.json (texte observé)
</context>

<constraints>
- **Contrat install partagé (voir NOTE frontmatter)** : le TreeAudit LIT `localStorage['skilltree.installs.v1']` (record de slugs) et écoute l'event `skilltree:installs`, via un **helper local** (ne PAS importer `lib/installs.ts` de 03-01). Garder aussi le fallback actuel (jobs autonomes détectés) comme « déjà sur la machine », mais le compteur d'installs doit refléter les installs réels.
- **Chrome UI réécrit FR, jamais verbatim** : libellés FR fidèles en structure aux captures.
- **Aucun chiffre inventé non étiqueté** : les stats du Tree qui n'ont pas de source (drops, fréquence sync) restent des libellés de démo étiquetés ou neutres ; le compteur « skills installés » = compte réel des installs locaux.
- **Stripe-ready, pas actif (D8)** : la section Facturation = lien/placeholder « Gérer la facturation sur Stripe » sans intégration réelle. Plan « Membre · 47 $/mois » affiché.
- **A11y** : boutons focusables, `aria-label` sur ConnectMachine (copier), régions labellisées.
- **Anti-faux-positif** : dom-assert.txt cite les mesures réelles (compteur N→N+1). Jamais `|| true`. Serveur de preuve : `cd apps/web && pnpm build` (exit 0, ne pas piper) puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python`, style `tools/verify_p1.py`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: My Tree — ConnectMachine + stats + WeeklyDrops + TreeAudit réactif aux installs</name>
  <files>apps/web/app/(app)/tree/page.tsx, apps/web/components/tree/MyTree.tsx, apps/web/components/tree/TreeAudit.tsx, apps/web/components/tree/WeeklyDrops.tsx</files>
  <action>
Regarder `captures/nav_tree.png` + `captures/content/nav_tree.json` avant de coder.

1. **Helper local `useInstalledSlugs()`** (dans `TreeAudit.tsx` ou un petit module de ce plan, PAS lib/installs.ts) : lit `localStorage['skilltree.installs.v1']` (record), écoute `window` event `skilltree:installs` + `storage`, renvoie `string[]`. Clone le style de `lib/progress.ts`.
2. `TreeAudit.tsx` : « SkillTree Audit — ton arbre, calculé ». Compteur « skills installés » = `useInstalledSlugs().length`, réactif (pas de reload). Liste les jobs installés (résolus via `skillBySlug`) + garde éventuellement la détection « déjà sur la machine » (jobs autonomes) comme section distincte étiquetée.
3. `WeeklyDrops.tsx` : bandeau « Drops de la semaine » (réutilise la sélection déterministe ; pas de compteur inventé).
4. `MyTree.tsx` : composer ConnectMachine (commande git + copier, existant), rangée de stats (dont « Skills installés » = compteur réactif), WeeklyDrops, TreeAudit. Titres FR fidèles.
  </action>
  <verify>`pnpm build` exit 0 ; en dev : `/tree` montre les 4 blocs ; dans la console, `localStorage.setItem('skilltree.installs.v1', JSON.stringify({'billing-manager':true})); window.dispatchEvent(new Event('skilltree:installs'))` → le compteur « skills installés » passe à 1 sans reload.</verify>
  <done>My Tree fidèle, TreeAudit réagit aux installs locaux (compteur réactif).</done>
</task>

<task type="auto">
  <name>Task 2: Community + Settings fidèles aux captures</name>
  <files>apps/web/app/(app)/community/page.tsx, apps/web/app/(app)/settings/page.tsx, apps/web/components/settings/SettingsSection.tsx</files>
  <action>
Regarder `captures/nav_community.png` + `captures/nav_settings.png` (+ leurs .json) avant de coder.

1. `/community` : bandeau « Tu fais partie des 100 premiers… » + `CommunityFeed` (composer + posts existants). Vérifier la fidélité structurelle (avatars initiales, âge relatif, réactions).
2. `/settings` : sections Compte (avatar + email + plan **« Membre · 47 $/mois »**), Sécurité (PasswordForm), Facturation (placeholder Stripe « Gérer la facturation sur Stripe », D8 — pas d'intégration), Zone de danger (LogoutButton). S'assurer que le libellé « Membre · 47 $/mois » est présent et lisible. Titres FR fidèles à la capture.
3. Ajuster `SettingsSection.tsx` si besoin (structure de carte fidèle).
  </action>
  <verify>`pnpm build` exit 0 ; en dev : `/community` = bandeau + feed ; `/settings` = 4 sections avec « Membre · 47 $/mois » visible.</verify>
  <done>Community et Settings fidèles aux captures, Member 47$/mo affiché, billing en placeholder Stripe-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Preuves Playwright — orchestration/verify/p5/</name>
  <files>tools/verify_p5.py, orchestration/verify/p5/dom-assert.txt, orchestration/verify/p5/tree.png, orchestration/verify/p5/community.png, orchestration/verify/p5/settings.png</files>
  <action>
Écrire `tools/verify_p5.py` (Playwright sync, `/home/nuveo/.higgsfield-login-venv/bin/python`, serveur :3010 up), style `tools/verify_p1.py`, PASS/FAIL + valeur, exit 1 au 1er FAIL. Nettoyer `localStorage['skilltree.installs.v1']` au début.
1. **Tree** : `/tree` → asserter présence ConnectMachine (la commande git), la rangée de stats, TreeAudit. Screenshot `tree.png`.
2. **TreeAudit avant/après install** : lire le compteur « skills installés » (N, attendu 0) → `page.evaluate` : écrire `localStorage['skilltree.installs.v1']` avec 1 slug réel + `window.dispatchEvent(new Event('skilltree:installs'))` → asserter le compteur = N+1 **sans reload**. Consigner N et N+1.
3. **Community** : `/community` → bandeau « 100 premiers » + au moins 1 post. Screenshot `community.png`.
4. **Settings** : `/settings` → asserter le texte « Membre · 47 $/mois » (ou « 47 $ »/« Member ») présent + les 4 sections. Screenshot `settings.png`.

Écrire `dom-assert.txt` : date, commandes serveur, chaque check + mesure citée (compteur N→N+1, texte Member) + PASS/FAIL, + verdicts fidélité (tree/community/settings vs captures). Tuer le serveur. Si FAIL → corriger, relancer ; jamais éditer le txt à la main.
  </action>
  <verify>`/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p5.py` (serveur :3010) → tous PASS, exit 0 ; les 3 PNG > 30 Ko ; dom-assert.txt cite le compteur TreeAudit N→N+1 et « 47 $ ».</verify>
  <done>Les 3 vues fidèles prouvées sur rendu réel, TreeAudit réactif aux installs prouvé (compteur avant/après), preuves reproductibles dans orchestration/verify/p5/.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert.
- `tools/verify_p5.py` exit 0 : 3 screenshots vs captures, TreeAudit N→N+1 sur install, « Membre · 47 $/mois » présent.
- `orchestration/verify/p5/` : dom-assert.txt + tree.png + community.png + settings.png.
</verification>

<success_criteria>
1. Screenshots vs `nav_tree.png` / `nav_community.png` / `nav_settings.png` (Criterion 4 de phase).
2. TreeAudit change quand on installe un skill (compteur avant/après).
3. Membre · 47 $/mois affiché. Preuves dans `orchestration/verify/p5/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-vues-modules-ui/03-04-SUMMARY.md`
</output>
