---
phase: 03-vues-modules-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/modules/page.tsx
  - apps/web/app/(app)/modules/[module]/page.tsx
  - apps/web/app/(app)/modules/[module]/[lesson]/page.tsx
  - apps/web/components/lesson/Stepper.tsx
  - apps/web/components/lesson/LessonReader.tsx
  - apps/web/components/lesson/LessonNav.tsx
  - apps/web/components/lesson/ProgressBar.tsx
  - apps/web/content/lessons/
  - orchestration/verify/p3/coverage.md
  - orchestration/verify/p3/dom-assert.txt
  - orchestration/verify/p3/lesson-long.png
  - orchestration/verify/p3/stepper-advance.png
  - tools/verify_p3.py

autonomous: true

# NOTE modèle/effort (GSD) : Task 1 (lookups leçon↔capture, matrice) = haiku-4-5 / low.
# Task 2 (compléments FR + wiring stepper) = sonnet-4-6 / medium. Task 3 (preuves) = sonnet-4-6 / medium.
# Revue fable/opus avant intégration.
# INTERFACE stub : la progression utilise lib/progress.ts EXISTANT (localStorage
# `skilltree.progress.v1`) — NE PAS réinventer, seulement consommer/étendre. Ce plan ne
# touche PAS lib/progress.ts sauf ajout non-cassant ; aucun autre plan ne le modifie.

must_haves:
  truths:
    - "orchestration/verify/p3/coverage.md contient une matrice 18/18 : chaque leçon (module/slug) → capture source (captures/lessons/*), sections couvertes, verdict (aligné / complété / manquant→réécrit)"
    - "Les 18 leçons rendent dans /modules/[module]/[lesson] avec titre, sections, callouts et blocs code ; toute leçon dont la capture montre des sections absentes est complétée en FR (jamais verbatim)"
    - "Marquer une leçon « terminée » avance le stepper (compteur X/N du module augmente) et l'état persiste au reload (localStorage `skilltree.progress.v1`)"
    - "La navigation d'un module au clic parcourt les leçons dans l'ordre (LessonNav prev/next, y compris transition inter-modules)"
    - "Spot-check anti-verbatim : sur 2 leçons complétées, aucune phrase de ≥ 8 mots identique au texte de la capture source"
    - "Preuves dans orchestration/verify/p3/ : coverage.md + dom-assert.txt cité + lesson-long.png + stepper-advance.png"
  artifacts:
    - path: "orchestration/verify/p3/coverage.md"
      provides: "matrice 18/18 leçon↔capture (source, sections, verdict) — le livrable central du critère MOD-01"
      min_lines: 40
    - path: "apps/web/components/lesson/Stepper.tsx"
      provides: "stepper de module : leçons ordonnées, état done/actif/à-venir, compteur X/N branché sur useProgress()"
      min_lines: 30
    - path: "tools/verify_p3.py"
      provides: "Playwright : mark done → stepper avance + reload persiste, nav complète, screenshots, anti-verbatim"
      min_lines: 60
    - path: "orchestration/verify/p3/dom-assert.txt"
      provides: "preuve citée : compteur stepper avant/après, persistance reload, 2 verdicts anti-verbatim"
  key_links:
    - from: "apps/web/components/lesson/Stepper.tsx"
      to: "apps/web/lib/progress.ts"
      via: "useProgress().countDone / isComplete pour l'état des leçons"
      pattern: "useProgress|countDone"
    - from: "apps/web/components/lesson/LessonReader.tsx"
      to: "apps/web/lib/progress.ts"
      via: "markComplete(module, lesson) au bouton « Marquer comme terminé »"
      pattern: "markComplete"
    - from: "apps/web/app/(app)/modules/[module]/[lesson]/page.tsx"
      to: "apps/web/lib/lessons.ts"
      via: "getLesson(module, lesson) + getAdjacent pour le contenu et la nav"
      pattern: "getLesson"
---

<objective>
Auditer les 18 leçons FR existantes contre `captures/lessons/*` (matrice de couverture structurelle), compléter en FR ce qui manque (jamais verbatim), et brancher le parcours : stepper 01 Commence ici (8) / 02 Fondations (5) / 03 Construis ton Brain (5), LessonReader, LessonNav, progression locale qui avance et persiste. Preuves sur rendu réel dans `orchestration/verify/p3/`.

Purpose: MOD-01 (matrice 18/18 + fidélité structurelle), MOD-02 (stepper + progression) — P3 docs/PLAN.md.
Output: matrice `coverage.md`, leçons complétées, Stepper/LessonReader/LessonNav branchés sur `lib/progress.ts`, `tools/verify_p3.py`, preuves `orchestration/verify/p3/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md            (§3.2 Modules)
@docs/07_VISUAL_SPEC.md
@apps/web/lib/lessons.ts         (ORDER + CONTENT des 18 leçons, getLesson/getAdjacent/getLessons — source de vérité ordre+slug)
@apps/web/lib/progress.ts        (useProgress : isComplete/markComplete/countDone — INTERFACE progression à consommer)
@apps/web/lib/lesson-types.ts    (LessonContent : forme d'une leçon)
@apps/web/content/lessons/       (18 leçons FR existantes — à auditer et compléter)

Références visuelles (regarder AVANT de coder) :
- captures/nav_modules.png (index modules), captures/lessons/modules*.json (texte)
- captures/lessons/modules_foundations_claude_md.png / _how_it_works.png (+ leurs .json) = leçons longues de référence pour la structure (sections, callouts, blocs code)
</context>

<constraints>
- **Contenu FR réécrit, jamais verbatim** : les 18 leçons existent déjà en FR ; les compléments (sections manquantes vs capture) sont écrits en prose FR neuve. Aucune phrase copiée du texte des captures. Spot-check anti-verbatim obligatoire dans les preuves.
- **Fidélité structurelle, pas mot-à-mot** : la matrice compare la STRUCTURE (sections, callouts, blocs code présents) — pas la traduction exacte. Une leçon est « alignée » si toutes les sections structurelles de la capture existent ; « complétée » si on en a ajouté ; « manquante→réécrit » si absente.
- **Interface progression existante** : consommer `lib/progress.ts` (localStorage `skilltree.progress.v1` + event). NE PAS créer un second système de progression. Phase 4 remplacera le corps de progress.ts par `/api/progress` — l'UI ne doit pas dépendre de localStorage directement, seulement du hook `useProgress()`.
- **A11y** : boutons focusables, LessonNav au clavier, `aria-current` sur l'étape active du stepper.
- **Anti-faux-positif** : chaque assert de `dom-assert.txt` cite la mesure réelle (compteur avant/après). Jamais `|| true`. Serveur de preuve : `cd apps/web && pnpm build` (exit 0, ne pas piper) puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python`, style `tools/verify_p1.py`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Audit couverture 18/18 → coverage.md (lookups leçon↔capture)</name>
  <files>orchestration/verify/p3/coverage.md</files>
  <action>
**haiku-4-5 / low — lookups de correspondance.** Pour chacune des 18 leçons (ordre de `lib/lessons.ts` : start-here×8, foundations×5, second-brain×5) :
1. Lire le contenu FR existant (`apps/web/content/lessons/<module>/<slug>`).
2. Trouver la capture correspondante dans `captures/lessons/*` (PNG + .json de texte ; toutes les leçons n'ont pas de capture dédiée — noter « pas de capture dédiée » le cas échéant, la référence structurelle générique reste `modules_foundations_claude_md`/`_how_it_works`).
3. Comparer la STRUCTURE : sections (titres), callouts, blocs code, listes ordonnées.
Écrire `orchestration/verify/p3/coverage.md` : un tableau 18 lignes `| # | module/slug | capture source | sections capture | sections leçon | verdict |` avec verdict ∈ {aligné, à compléter (liste des sections manquantes), manquant→réécrit}. Ce fichier PILOTE la Task 2.
  </action>
  <verify>`grep -c "^|" orchestration/verify/p3/coverage.md` ≥ 19 (en-tête + 18 lignes) ; chaque ligne cite un verdict ; les 18 slugs de `lib/lessons.ts` sont présents.</verify>
  <done>Matrice 18/18 écrite, gaps structurels identifiés par leçon.</done>
</task>

<task type="auto">
  <name>Task 2: Compléter les leçons FR manquantes + brancher stepper/reader/nav + progression</name>
  <files>apps/web/content/lessons/ (leçons à compléter), apps/web/components/lesson/Stepper.tsx, apps/web/components/lesson/LessonReader.tsx, apps/web/components/lesson/LessonNav.tsx, apps/web/components/lesson/ProgressBar.tsx, apps/web/app/(app)/modules/page.tsx, apps/web/app/(app)/modules/[module]/page.tsx, apps/web/app/(app)/modules/[module]/[lesson]/page.tsx</files>
  <action>
1. **Compléter le contenu** : pour chaque leçon `à compléter`/`manquant→réécrit` de `coverage.md`, ajouter les sections manquantes en **FR neuf** (respecter `lib/lesson-types.ts` : LessonContent). Ne pas toucher les leçons `aligné`.
2. **Stepper.tsx** : liste ordonnée des leçons du module (via `getLessons`), état par leçon (done ✓ / active / à venir) depuis `useProgress().isComplete`, compteur `X / N` via `countDone`, `aria-current` sur l'active, clic → navigue vers la leçon.
3. **LessonReader.tsx** : rend le `LessonContent` (titre, sections, `Callout`, `CodeBlock`) + bouton **« Marquer comme terminé »** → `markComplete(module, slug)` (idempotent). Après marquage, l'état se reflète immédiatement (le hook re-render).
4. **LessonNav.tsx** : prev/next via `getAdjacent` (y compris transition inter-modules), au clavier.
5. **ProgressBar.tsx** + pages : `/modules` (index 3 cartes + progression par module), `/modules/[module]` (stepper + entrée leçon), `/modules/[module]/[lesson]` (reader + nav + stepper latéral). Titres/labels FR fidèles à `captures/nav_modules.png`.
  </action>
  <verify>`pnpm build` exit 0 ; en dev : ouvrir une leçon, « Marquer comme terminé » → le compteur du module passe X→X+1 et l'étape du stepper devient ✓ ; recharger la page → toujours ✓ ; prev/next parcourt tout le module puis passe au module suivant.</verify>
  <done>18 leçons rendues et complétées, stepper/reader/nav branchés, progression avance et persiste via useProgress().</done>
</task>

<task type="auto">
  <name>Task 3: Preuves Playwright — orchestration/verify/p3/</name>
  <files>tools/verify_p3.py, orchestration/verify/p3/dom-assert.txt, orchestration/verify/p3/lesson-long.png, orchestration/verify/p3/stepper-advance.png</files>
  <action>
Écrire `tools/verify_p3.py` (Playwright sync, `/home/nuveo/.higgsfield-login-venv/bin/python`, serveur :3010 up), style `tools/verify_p1.py`, PASS/FAIL + valeur, exit 1 au 1er FAIL :
1. **Stepper avance + persiste** : aller sur une leçon d'un module, lire le compteur du stepper (`X / N`), cliquer « Marquer comme terminé », asserter le compteur = `X+1` et l'étape marquée `aria-current`/✓ ; screenshot `stepper-advance.png` ; **reload** la page → asserter que le compteur reste `X+1` (lecture `localStorage['skilltree.progress.v1']` via `page.evaluate` + état visuel). Nettoyer la clé au début du run pour partir d'un état connu.
2. **Navigation complète** : depuis la 1re leçon d'un module, cliquer next jusqu'au bout puis vérifier la transition vers le module suivant (LessonNav). 
3. **Leçon longue** : ouvrir une leçon longue (ex. foundations/claude-md) → asserter présence de ≥ 2 sections + ≥ 1 bloc code + ≥ 1 callout ; screenshot `lesson-long.png`.
4. **Anti-verbatim** : pour 2 leçons complétées en Task 2, comparer hors-browser au texte de la capture source (si dédiée) — asserter aucune fenêtre de 8 mots identiques ; consigner les 2 verdicts dans `dom-assert.txt`.

Écrire `dom-assert.txt` : date, commandes serveur, chaque check + mesure citée (compteurs avant/après) + PASS/FAIL, + verdict fidélité `lesson-long.png` vs capture. Tuer le serveur. Si FAIL → corriger, relancer ; jamais éditer le txt à la main.
  </action>
  <verify>`/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p3.py` (serveur :3010) → tous PASS, exit 0 ; `lesson-long.png` + `stepper-advance.png` > 30 Ko ; dom-assert.txt cite le compteur X→X+1 et 2 verdicts anti-verbatim ; coverage.md présent avec 18 lignes.</verify>
  <done>Progression prouvée sur rendu réel (avance + persiste), couverture 18/18 documentée, preuves reproductibles dans orchestration/verify/p3/.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert.
- `tools/verify_p3.py` exit 0 : stepper X→X+1 + persistance reload, nav complète, leçon longue structurée, 2 verdicts anti-verbatim.
- `orchestration/verify/p3/` : coverage.md (18 lignes) + dom-assert.txt + lesson-long.png + stepper-advance.png.
</verification>

<success_criteria>
1. Matrice 18/18 leçon↔capture dans `orchestration/verify/p3/coverage.md` (Criterion 2 de phase).
2. Marquer une leçon terminée avance le stepper et persiste au reload (assert DOM + localStorage).
3. Aucun verbatim (spot-check 2 leçons). Preuves dans `orchestration/verify/p3/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-vues-modules-ui/03-02-SUMMARY.md`
</output>
