---
phase: 03-vues-modules-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/map/MapView.tsx
  - apps/web/components/dashboards/CommandCenters.tsx
  - apps/web/components/dashboards/StatTile.tsx
  - apps/web/components/chart/RolloutMatrix.tsx
  - apps/web/components/chart/JobCard.tsx
  - apps/web/lib/chart.ts
  - tools/verify_p51.py
  - orchestration/verify/p51/dom-assert.txt
  - orchestration/verify/p51/dashboards.png
  - orchestration/verify/p51/chart.png

autonomous: true

# NOTE modèle/effort (GSD) : sonnet-4-6 / medium. Revue fable/opus avant intégration.
# WIRING : MapView.tsx a déjà 2 branches placeholder (view==='dashboards' / 'chart',
# EmptyState). Ce plan REMPLACE ces 2 branches par <CommandCenters/> et <RolloutMatrix/>.
# MapView.tsx est un fichier de la Phase 2 ; en Phase 3 SEUL ce plan (03-05) le modifie
# → pas de conflit avec les 4 autres plans. Le switcher ViewSwitcher (?view=) est déjà en place.

must_haves:
  truths:
    - "?view=dashboards affiche les 6 command centers de data/dashboards.json (Meta Ads, HubSpot Pipeline, Content, Outbound, Client Delivery, Xero Finance) : rangée de stat tiles (valeur + delta coloré), tables ; nav dashboard précédent/suivant"
    - "Les chiffres des dashboards sont IDENTIQUES à la donnée déterministe (seed 20260611) : ex. Meta Ads « $31,587 » / « 1,049 » leads / delta « 19.0% » — lus depuis DASHBOARDS, jamais réinventés"
    - "?view=chart affiche la matrice rollout : 7 tabs secteurs dont Sales, colonnes = 4 stages nommés, lignes = 3 niveaux (Human-led / Human-assisted / Fully autonomous), cartes job positionnées"
    - "Le résumé « N of M jobs run autonomously · X assisted · Y human-led » est RECALCULÉ depuis la donnée par lib/chart.ts (pas copié) : total tous secteurs = 165, human-led total = 36 ; par secteur les comptes = ceux de assert-graph (ex. Deals 19/28 auto, 4 assisted, 5 human-led ; Sales 19/26 auto, 3 assisted, 4 human-led)"
    - "Les extras origin='chart' (28) sont inclus dans la vue CHART uniquement ; la roue MAP reste à 137"
    - "Preuves dans orchestration/verify/p51/ : dom-assert.txt cité (chiffres dashboards = données, N of M recalculés = valeurs assert-graph) + dashboards.png + chart.png"
  artifacts:
    - path: "apps/web/lib/chart.ts"
      provides: "recompute des résumés CHART depuis CHART_JOBS : par secteur {total, autonomous, assisted, humanLed} + totaux (165 / 36 human-led) + regroupement stage×level pour la matrice"
      min_lines: 40
    - path: "apps/web/components/dashboards/CommandCenters.tsx"
      provides: "les 6 command centers depuis DASHBOARDS : stat tiles + deltas + tables + nav prev/next, démo déterministe étiquetée"
      min_lines: 70
    - path: "apps/web/components/chart/RolloutMatrix.tsx"
      provides: "matrice 7 secteurs (tabs) × 4 stages × 3 niveaux, résumé N of M recalculé via lib/chart.ts, extras origin='chart' inclus"
      min_lines: 80
    - path: "tools/verify_p51.py"
      provides: "Playwright : chiffres dashboards = données, N of M chart = valeurs recalculées (165/36 + par secteur), 7 secteurs dont Sales, screenshots"
      min_lines: 70
    - path: "orchestration/verify/p51/dom-assert.txt"
      provides: "preuve citée : valeurs dashboards vs DASHBOARDS, N of M recalculés vs assert-graph (par secteur + totaux 165/36)"
  key_links:
    - from: "apps/web/components/map/MapView.tsx"
      to: "apps/web/components/dashboards/CommandCenters.tsx & apps/web/components/chart/RolloutMatrix.tsx"
      via: "remplacement des 2 branches placeholder view==='dashboards'/'chart'"
      pattern: "CommandCenters|RolloutMatrix"
    - from: "apps/web/components/chart/RolloutMatrix.tsx"
      to: "apps/web/lib/chart.ts"
      via: "sectorSummary(sector) pour le « N of M » recalculé (jamais copié de la capture)"
      pattern: "sectorSummary|lib/chart"
    - from: "apps/web/components/dashboards/CommandCenters.tsx"
      to: "apps/web/lib/catalog (DASHBOARDS)"
      via: "les 6 CommandCenter (stats/tables) lus depuis la donnée déterministe"
      pattern: "DASHBOARDS"
---

<objective>
Livrer les 2 dernières vues du hero derrière le switcher `?view=` (D6) : **DASHBOARDS** (6 command centers de `data/dashboards.json`, démo déterministe seed 20260611, chiffres identiques aux captures) et **CHART** (matrice rollout stage×level, 7 secteurs, extras `origin='chart'` inclus, résumés « N of M » **recalculés** depuis la donnée = valeurs assert-graph 165/36, jamais copiés). Preuves sur rendu réel dans `orchestration/verify/p51/`.

Purpose: DASH-01 (6 command centers), CHART-01 (matrice 7 secteurs), CHART-02 (N of M recalculés) — P5.1 docs/PLAN.md.
Output: `CommandCenters` + `RolloutMatrix` branchés dans `MapView`, `lib/chart.ts`, `tools/verify_p51.py`, preuves `orchestration/verify/p51/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md            (§3.1 DASHBOARDS/CHART, §7 réconciliation, D2/D3/D6)
@docs/07_VISUAL_SPEC.md          (§Vue DASHBOARDS + §Vue CHART — structure exacte observée)
@apps/web/components/map/MapView.tsx      (2 branches placeholder view==='dashboards'/'chart' à REMPLACER)
@apps/web/lib/catalog/index.ts   (DASHBOARDS=6 command centers, CHART_JOBS=165, SKILLS=137, sectorOf)
@apps/web/lib/catalog/types.ts   (CommandCenter {id,short,name,sub,color,range_days,stats[],tables}, Skill {level,stage,stageName,origin,sector})

Références visuelles (regarder AVANT de coder) :
- captures/dashboards/*_full.png (6) + *_full.json (structure stats/tables exacte) + captures/dashboards/dash_text.json
- captures/chart/{sector}__matrix.png + __expanded.png (7) + captures/chart/{sector}.json (stages, cards, summaries source)
- captures/dynamic/chart_html.png
</context>

<constraints>
- **Chiffres = donnée, jamais réinventés (D3)** : les stats/tables des dashboards sont lues telles quelles depuis `DASHBOARDS` (data/dashboards.json, seed 20260611). Étiqueter la vue « démo » (comme l'original). Ne jamais fabriquer un chiffre.
- **N of M RECALCULÉ, pas copié (CHART-02)** : `lib/chart.ts` calcule par secteur `{autonomous, assisted, humanLed, total}` depuis `CHART_JOBS.filter(sector)` — le texte « N of M jobs run autonomously · X assisted · Y stay human-led » est composé à partir de ces valeurs. Ne PAS lire le résumé texte des captures `chart/*.json`. Totaux attendus (contrôle) : 165 jobs, 36 human-led (= sortie assert-graph). NE PAS coder ces nombres en dur : ils DOIVENT tomber du calcul.
- **D2 respecté** : la vue CHART inclut les 165 (137 map + 28 extras `origin='chart'`) ; la roue MAP reste à 137 — ne pas mélanger. Les extras human-led (badge « Ongoing » dans l'original) apparaissent dans la ligne Human-led.
- **Stages Sales** : `data/chart.json` a `Sales.stages=[]` (trou de capture) → utiliser le `stageName` de `skills.json` (déjà mergé dans le catalogue) comme fallback pour nommer les colonnes Sales ; jamais inventer un nom de stage.
- **A11y** : tabs secteurs = navigation clavier + `aria-selected` ; stat tiles labellisés ; cartes job focusables.
- **Anti-faux-positif** : dom-assert.txt cite les valeurs réelles (chiffres dashboards lus + N of M recalculés). Jamais `|| true`. Serveur de preuve : `cd apps/web && pnpm build` (exit 0, ne pas piper) puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python`, style `tools/verify_p1.py`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: lib/chart.ts + RolloutMatrix (CHART) — 7 secteurs, N of M recalculés</name>
  <files>apps/web/lib/chart.ts, apps/web/components/chart/RolloutMatrix.tsx, apps/web/components/chart/JobCard.tsx</files>
  <action>
Regarder `captures/chart/deals__matrix.png` + `sales__matrix.png` (+ leurs .json) avant de coder.

1. `lib/chart.ts` :
   - `sectorSummary(sectorSlug)` : filtre `CHART_JOBS` par secteur, renvoie `{ total, autonomous, assisted, humanLed }` (comptés depuis `level`). Le libellé « N of M jobs run autonomously · X assisted · Y stay human-led » est composé à partir de ces comptes — réécrit FR (« N jobs sur M tournent en autonomie · X assistés · Y restent pilotés par un humain »).
   - `chartTotals()` : `{ total, humanLed }` sur tout `CHART_JOBS` (contrôle : 165 / 36).
   - `stagesFor(sectorSlug)` : les 4 noms de stage (depuis les `stageName` distincts 1-4 du secteur ; fallback pour Sales via `stageName` de skills.json déjà mergé). `groupByStageLevel(sectorSlug)` : regroupe les jobs en grille stage(1-4)×level(3), les human-led sans stage → bande « Ongoing ».
2. `JobCard.tsx` : carte job (icône + nom FR/affichage + badge « stage · dots » ou « Ongoing » pour human-led), focusable.
3. `RolloutMatrix.tsx` : tabs 7 secteurs (`aria-selected`), titre « <Secteur> · le déploiement IA », **résumé recalculé** via `sectorSummary`, légende 3 niveaux, colonnes = 4 stages (via `stagesFor`), lignes = 3 bandes level, cartes positionnées via `groupByStageLevel`. Inclut les extras `origin='chart'`.
  </action>
  <verify>`pnpm build` exit 0 ; `node -e "..."` ou test : la somme des `sectorSummary().total` sur 7 secteurs = 165 et la somme `humanLed` = 36 ; en dev `?view=chart` (après Task 3 wiring) montre 7 tabs dont Sales. `grep -n "165\|36" apps/web/lib/chart.ts` → 0 (les nombres ne sont PAS en dur).</verify>
  <done>Matrice CHART 7 secteurs avec résumés N of M recalculés depuis la donnée (165/36 tombent du calcul), extras chart inclus.</done>
</task>

<task type="auto">
  <name>Task 2: CommandCenters (DASHBOARDS) — 6 centers, chiffres = données</name>
  <files>apps/web/components/dashboards/CommandCenters.tsx, apps/web/components/dashboards/StatTile.tsx</files>
  <action>
Regarder `captures/dashboards/meta-ads-paid-acquisition_full.png` + `_full.json` (+ les 5 autres) avant de coder.

1. `StatTile.tsx` : icône/label + valeur XL + delta coloré (↗ vert / ↘ rouge selon le signe de `delta`) + phrase d'état. (Sparkline optionnelle — si non triviale, l'omettre proprement plutôt que d'inventer des points.)
2. `CommandCenters.tsx` : lit les 6 `DASHBOARDS` (data/dashboards.json). Pour le center actif : `← Tous les dashboards`, titre + sous-titre, sélecteur période (pill 7d/14d/30d/90d, visuel), **rangée de stat tiles** depuis `center.stats` (valeur + delta), puis les `center.tables` rendues en tables simples (campagnes, leaderboard…). Nav dashboard précédent/suivant (libellés + chevrons). Étiquette « démo déterministe » discrète. Toutes les valeurs viennent de la donnée — aucune fabriquée.
  </action>
  <verify>`pnpm build` exit 0 ; en dev `?view=dashboards` (après Task 3 wiring) : le 1er center (Meta Ads) montre « $31,587 » et « 1,049 » ; nav prev/next parcourt les 6. `grep -n "31,587\|1,049" apps/web/components/dashboards/CommandCenters.tsx` → 0 (lu depuis DASHBOARDS, pas en dur).</verify>
  <done>6 command centers rendus depuis la donnée déterministe, stat tiles + tables + nav, aucun chiffre inventé.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring MapView + preuves Playwright — orchestration/verify/p51/</name>
  <files>apps/web/components/map/MapView.tsx, tools/verify_p51.py, orchestration/verify/p51/dom-assert.txt, orchestration/verify/p51/dashboards.png, orchestration/verify/p51/chart.png</files>
  <action>
1. **Wiring** : dans `MapView.tsx`, remplacer la branche `view === "dashboards"` par `<CommandCenters/>` et la branche `view === "chart"` par `<RolloutMatrix/>` (retirer les 2 EmptyState placeholder). Garder la branche MAP intacte.
2. **Preuves** : écrire `tools/verify_p51.py` (Playwright sync, `/home/nuveo/.higgsfield-login-venv/bin/python`, serveur :3010 up), style `tools/verify_p1.py`, PASS/FAIL + valeur, exit 1 au 1er FAIL :
   a. **Dashboards** : `/map?view=dashboards` → asserter la présence des valeurs de la donnée pour le 1er center (lire `DASHBOARDS[0].stats` dans le script pour comparer — ex. « $31,587 », « 1,049 ») ; parcourir prev/next et confirmer 6 centers distincts. Screenshot `dashboards.png`.
   b. **Chart 7 secteurs** : `/map?view=chart` → asserter 7 tabs, dont un intitulé « Sales »/« Ventes » ; cliquer 2-3 tabs sans erreur. Screenshot `chart.png`.
   c. **N of M recalculés** : pour 2 secteurs (ex. Deals, Sales), calculer côté script depuis `CHART_JOBS` (ou depuis le catalog.json chargé) `{auto, assisted, humanLed, total}`, et asserter que le résumé affiché dans la matrice contient EXACTEMENT ces nombres (ex. Deals → 19 / 28 / 4 / 5). Asserter aussi les totaux : somme sur 7 secteurs = 165, human-led = 36.
   d. **Isolation MAP** : `/map` (roue) reste à 137 nœuds (`[data-node]`).
   Écrire `dom-assert.txt` : date, commandes serveur, chaque check + valeurs citées (chiffres dashboards lus, N of M recalculés par secteur + totaux 165/36) + PASS/FAIL, + verdicts fidélité (dashboards vs `captures/dashboards/*_full.png`, chart vs `captures/chart/*__matrix.png`). Tuer le serveur. Si FAIL → corriger, relancer ; jamais éditer le txt à la main.
  </action>
  <verify>`/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p51.py` (serveur :3010) → tous PASS, exit 0 ; `dashboards.png` + `chart.png` > 30 Ko ; dom-assert.txt cite les valeurs dashboards = données et les N of M recalculés (par secteur + 165/36).</verify>
  <done>Les 2 vues branchées derrière le switcher, chiffres dashboards = données et N of M chart recalculés prouvés sur rendu réel, preuves reproductibles dans orchestration/verify/p51/.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert (prebuild assert-graph : 165/36 confirmés à la source).
- `tools/verify_p51.py` exit 0 : chiffres dashboards = données, N of M chart recalculés = valeurs assert-graph (par secteur + totaux 165/36), 7 secteurs dont Sales, MAP toujours à 137.
- `orchestration/verify/p51/` : dom-assert.txt + dashboards.png + chart.png.
</verification>

<success_criteria>
1. 6 command centers, chiffres identiques aux captures (démo déterministe seed 20260611) (Criterion 5 de phase).
2. Chart : 7 secteurs dont Sales, résumés « N of M » recalculés = valeurs assert-graph (165 total / 36 human-led, pas copiés).
3. Extras origin='chart' inclus dans CHART uniquement, MAP reste à 137. Preuves dans `orchestration/verify/p51/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-vues-modules-ui/03-05-SUMMARY.md`
</output>
