---
phase: 03-vues-modules-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/brain/page.tsx
  - apps/web/components/brain/BrainBuilder.tsx
  - apps/web/components/brain/BrainWizard.tsx
  - apps/web/components/brain/BrainSection.tsx
  - apps/web/components/brain/BrainIntake.tsx
  - apps/web/lib/brain.ts
  - tools/verify_p4.py
  - orchestration/verify/p4/dom-assert.txt
  - orchestration/verify/p4/brain-section.png
  - orchestration/verify/p4/brain-manual.png

autonomous: true

# NOTE modèle/effort (GSD) : sonnet-4-6 / medium. Revue fable/opus avant intégration.
# INTERFACE stub : lib/brain.ts pose draftBrain() (stub local, PAS un appel LLM) +
# save par section en localStorage. Phase 4 remplace le corps de draftBrain() par
# POST /api/brain/draft (gateway :8765) et la persistance par /api/brain — signatures
# stables, l'UI ne change pas. Aucun autre plan ne touche lib/brain.ts.

must_haves:
  truths:
    - "/brain rend les 8 sections (company/offer/customers/voice/ops/stack/goals/constraints, noms FR calés sur captures/dynamic/brain_section_*) : une par écran, eyebrow « SECTION X/8 », question, éditeur, boutons Retour/Continuer"
    - "Écran d'entrée : deux chemins « Rédiger avec l'IA » vs « Je l'écris moi-même » (captures brain_initial / brain_manual) ; les deux mènent aux 8 sections"
    - "Éditer une section puis sauvegarder persiste au reload (localStorage `skilltree.brain.v1`) ; le badge source affiche « IA » ou « Manuel » selon le chemin utilisé pour cette section"
    - "Le chemin manuel complet est observable : choisir « Je l'écris moi-même » → parcourir et remplir les 8 sections → tout est sauvegardé, source=manual"
    - "draftBrain() est un stub local étiqueté (renvoie un gabarit FR par section, PAS un faux appel réseau) et pose la signature que la Phase 4 remplacera par la gateway :8765"
    - "Preuves dans orchestration/verify/p4/ : dom-assert.txt cité (8 sections, save→reload, badge source) + brain-section.png + brain-manual.png"
  artifacts:
    - path: "apps/web/lib/brain.ts"
      provides: "modèle 8 sections (clés + labels FR + questions), useBrain() (hook client : read/save par section, source ai|manual, localStorage `skilltree.brain.v1` + event), draftBrain(input) stub retournant un gabarit FR par section"
      min_lines: 60
    - path: "apps/web/components/brain/BrainWizard.tsx"
      provides: "wizard une-section-par-écran : eyebrow SECTION X/8, question, éditeur, Retour/Continuer, badge source, save par section"
      min_lines: 80
    - path: "apps/web/components/brain/BrainIntake.tsx"
      provides: "écran d'entrée : choix « Rédiger avec l'IA » (→ draftBrain) vs « Je l'écris moi-même » (→ sections vides, source=manual)"
      min_lines: 30
    - path: "tools/verify_p4.py"
      provides: "Playwright : 8 sections, save→reload persiste, badge source ai|manual, chemin manuel complet, screenshots"
      min_lines: 60
    - path: "orchestration/verify/p4/dom-assert.txt"
      provides: "preuve citée : 8 sections rendues, contenu persistant après reload, badge source par chemin"
  key_links:
    - from: "apps/web/components/brain/BrainWizard.tsx"
      to: "apps/web/lib/brain.ts"
      via: "useBrain().save(section, content, source) + read au montage"
      pattern: "useBrain|save\\("
    - from: "apps/web/components/brain/BrainIntake.tsx"
      to: "apps/web/lib/brain.ts"
      via: "draftBrain(input) au clic « Rédiger avec l'IA »"
      pattern: "draftBrain"
---

<objective>
Livrer le module Brain fidèle à `captures/dynamic/brain_*` : écran d'entrée à deux chemins (IA / manuel), wizard **8 sections** une-par-écran, éditeur + save par section (persistant localStorage), badge source `IA|Manuel`. Poser l'interface stub `draftBrain()` (gabarit local, pas d'appel LLM) que la Phase 4 remplacera par la gateway `:8765`. Preuves sur rendu réel dans `orchestration/verify/p4/`.

Purpose: BRAIN-01 (8 sections), BRAIN-02 (deux chemins), BRAIN-03 (save par section persistant) — P4 docs/PLAN.md.
Output: `/brain` (BrainIntake + BrainWizard + BrainSection), `lib/brain.ts` (modèle + useBrain + draftBrain stub), `tools/verify_p4.py`, preuves `orchestration/verify/p4/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md            (§3.2 Brain, §5 enum brain_section=company/offer/customers/voice/ops/stack/goals/constraints, §4 flux Brain, D7)
@docs/07_VISUAL_SPEC.md          (§Brain — wizard une-question-par-écran, footnote « Saved as you go »)
@apps/web/components/brain/BrainBuilder.tsx   (implémentation actuelle : intake + 8 sections en grille + localStorage — À REFONDRE en wizard une-par-écran ; reprendre la persistance)
@apps/web/lib/progress.ts        (PATTERN localStorage + event à cloner pour lib/brain.ts)

Références visuelles (regarder AVANT de coder) :
- captures/nav_brain.png, captures/dynamic/brain_initial.png (+ .json), captures/dynamic/brain_manual.png (+ .json)
- captures/dynamic/brain_section_1..8.png (+ brain_section_*.json = eyebrow SECTION + question exacte de chaque section)
</context>

<constraints>
- **8 sections, ordre + noms des captures** : lire `captures/dynamic/brain_section_1..8.json` pour l'eyebrow (COMPANY…) et la question de chaque écran ; clés stables = enum ARCHITECTURE §5 (`company, offer, customers, voice, ops, stack, goals, constraints`), labels + questions réécrits FR fidèles.
- **draftBrain() = stub étiqueté, PAS un faux appel LLM** : renvoie un gabarit FR par section (déterministe, à partir de l'input intake). Commentaire clair « Phase 4 : POST /api/brain/draft → gateway :8765 ». Pas de setTimeout déguisé en appel réseau, pas de faux streaming. Une section draftée porte source=`ai`, une section tapée porte source=`manual`.
- **Interface stub propre (Phase 4-ready)** : `useBrain()` expose `read(section)`, `save(section, content, source)`, `all()` — signatures stables pour swap vers `/api/brain`. Persistance localStorage `skilltree.brain.v1` (garder la clé existante de BrainBuilder pour ne pas perdre de compat) + custom event, miroir de `lib/progress.ts`.
- **A11y** : « une question par écran » = focus sur l'éditeur à l'arrivée sur chaque section ; Retour/Continuer au clavier ; `aria-label` sur la barre de progression.
- **Anti-faux-positif** : dom-assert.txt cite les mesures réelles (les 8 sections, le contenu après reload). Jamais `|| true`. Serveur de preuve : `cd apps/web && pnpm build` (exit 0, ne pas piper) puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python`, style `tools/verify_p1.py`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: lib/brain.ts — modèle 8 sections + useBrain + draftBrain stub</name>
  <files>apps/web/lib/brain.ts</files>
  <action>
Lire `captures/dynamic/brain_section_1..8.json` pour l'eyebrow + la question de chaque section.

Créer `lib/brain.ts` :
1. `BRAIN_SECTIONS` : tableau ordonné de 8 `{ key, label, question }` — key ∈ enum ARCHITECTURE §5, label/question FR réécrits d'après les captures (ex. `company` → « L'entreprise » / « Comment s'appelle ton entreprise ? »).
2. Type `BrainEntry = { content: string; source: 'ai' | 'manual' }`. Persistance : localStorage `skilltree.brain.v1` = `Record<sectionKey, BrainEntry>` + event `skilltree:brain` (cloner `lib/progress.ts`).
3. `useBrain()` : hook client → `{ read(key), save(key, content, source), all(), reset() }`, re-render sur event/storage.
4. `draftBrain(input: { url?: string; notes?: string })` : renvoie `Record<sectionKey, string>` — un **gabarit FR déterministe** par section dérivé de l'input (pas d'appel réseau). En-tête commentaire : stub local, remplacé Phase 4 par la gateway :8765.
  </action>
  <verify>`pnpm build` exit 0 ; `grep -n "skilltree.brain.v1\|draftBrain\|BRAIN_SECTIONS" apps/web/lib/brain.ts` → les 3 présents ; `BRAIN_SECTIONS.length === 8` (vérifiable par un petit `node -e` ou test de rendu).</verify>
  <done>Modèle 8 sections + interface useBrain + draftBrain stub posés, prêts pour le swap Phase 4.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard Brain — intake 2 chemins + 8 sections une-par-écran + save/source</name>
  <files>apps/web/app/(app)/brain/page.tsx, apps/web/components/brain/BrainBuilder.tsx, apps/web/components/brain/BrainWizard.tsx, apps/web/components/brain/BrainSection.tsx, apps/web/components/brain/BrainIntake.tsx</files>
  <action>
Regarder `captures/dynamic/brain_initial.png`, `brain_manual.png`, `brain_section_1..8.png` avant de coder.

1. `BrainIntake.tsx` : écran d'entrée titre « Brain » + sous-titre « une interview de 10 minutes l'écrit pour toi », deux CTA : **« Rédiger ma base avec l'IA »** (→ `draftBrain(intake)` remplit les 8 sections, source=`ai`) et **« Je l'écris moi-même »** (→ 8 sections vides, source=`manual`). L'IA demande d'abord url + notes (comme l'actuel BrainBuilder).
2. `BrainWizard.tsx` : machine à états `intake → section(1..8) → done`. Une section par écran : eyebrow « <LABEL> X/8 », question, `BrainSection` (éditeur), boutons **Retour / Continuer →**, barre de progression fine, footnote « Sauvegardé au fil de l'eau. C'est la base que chaque skill vient lire. Toi seul la vois. ». Focus sur l'éditeur à chaque arrivée.
3. `BrainSection.tsx` : textarea liée à `useBrain().read(key)` ; à la modification → `save(key, content, source)` (debounce court ou save sur blur/Continuer) ; **badge source** « IA » / « Manuel » visible.
4. `BrainBuilder.tsx` : devient l'orchestrateur (garde la persistance existante mais délègue au wizard) ; `brain/page.tsx` rend `<BrainBuilder/>`. Restaurer un brouillon existant au montage (reprendre à la 1re section non vide ou à l'intake si vide).
  </action>
  <verify>`pnpm build` exit 0 ; en dev : « Je l'écris moi-même » → parcourir les 8 écrans (eyebrow X/8 correct), taper dans une section, Continuer, revenir (Retour) → le texte est là ; recharger la page → le texte persiste ; le badge affiche « Manuel ». Refaire avec « Rédiger avec l'IA » → sections pré-remplies, badge « IA ».</verify>
  <done>Wizard 8 sections une-par-écran, deux chemins fonctionnels, save par section + badge source, persistance au reload.</done>
</task>

<task type="auto">
  <name>Task 3: Preuves Playwright — orchestration/verify/p4/</name>
  <files>tools/verify_p4.py, orchestration/verify/p4/dom-assert.txt, orchestration/verify/p4/brain-section.png, orchestration/verify/p4/brain-manual.png</files>
  <action>
Écrire `tools/verify_p4.py` (Playwright sync, `/home/nuveo/.higgsfield-login-venv/bin/python`, serveur :3010 up), style `tools/verify_p1.py`, PASS/FAIL + valeur, exit 1 au 1er FAIL. Nettoyer `localStorage['skilltree.brain.v1']` au début.
1. **8 sections** : chemin manuel → parcourir Continuer ×7 et asserter que l'eyebrow passe « X/8 » de 1 à 8 (compter 8 écrans distincts) ; screenshot d'une section `brain-section.png`.
2. **Save → reload persiste** : sur la section 1, taper un marqueur unique (ex. « ZZTEST-brain »), Continuer (déclenche save), `reload()` → revenir à la section 1 → asserter que le marqueur est présent (et `localStorage['skilltree.brain.v1']` le contient via `page.evaluate`).
3. **Badge source** : après chemin manuel, asserter le badge « Manuel » sur la section éditée. Puis reset, chemin IA (« Rédiger avec l'IA » + url/notes) → asserter sections pré-remplies + badge « IA ». Screenshot `brain-manual.png` (écran intake/manuel).
4. **Chemin manuel complet** : asserter les 8 clés présentes dans `localStorage['skilltree.brain.v1']` après remplissage.

Écrire `dom-assert.txt` : date, commandes serveur, chaque check + mesure citée + PASS/FAIL, + verdict fidélité `brain-section.png` vs `captures/dynamic/brain_section_1.png`. Tuer le serveur. Si FAIL → corriger, relancer ; jamais éditer le txt à la main.
  </action>
  <verify>`/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p4.py` (serveur :3010) → tous PASS, exit 0 ; `brain-section.png` + `brain-manual.png` > 30 Ko ; dom-assert.txt cite l'eyebrow 1→8, le marqueur persistant après reload, et le badge source par chemin.</verify>
  <done>8 sections + save/reload + badge source + chemin manuel complet prouvés sur rendu réel, preuves reproductibles dans orchestration/verify/p4/.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert.
- `tools/verify_p4.py` exit 0 : 8 sections (eyebrow 1→8), save→reload persiste, badge source ai|manual, chemin manuel complet.
- `orchestration/verify/p4/` : dom-assert.txt + brain-section.png + brain-manual.png.
</verification>

<success_criteria>
1. 8 sections rendues (assert DOM), édition+save d'une section persiste au reload (localStorage) (Criterion 3 de phase).
2. Badge source `ai|manual`, chemin manuel complet observé.
3. draftBrain() stub propre (Phase 4-ready, pas de faux appel LLM). Preuves dans `orchestration/verify/p4/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-vues-modules-ui/03-03-SUMMARY.md`
</output>
