---
phase: 03-vues-modules-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(app)/hub/page.tsx
  - apps/web/components/hub/FirstWeekChecklist.tsx
  - apps/web/components/hub/FreshDrops.tsx
  - apps/web/components/hub/FeaturedThisWeek.tsx
  - apps/web/components/hub/MostInstalled.tsx
  - apps/web/components/hub/BuildLogs.tsx
  - apps/web/components/hub/InstallModal.tsx
  - apps/web/components/ui/CommandBar.tsx
  - apps/web/lib/installs.ts
  - apps/web/lib/hub-data.ts
  - apps/web/lib/skill-files.ts
  - apps/web/content/skills/
  - tools/verify_p2.py
  - orchestration/verify/p2/dom-assert.txt
  - orchestration/verify/p2/hub.png
  - orchestration/verify/p2/cmdk-modal.png
  - orchestration/verify/p2/skill-files-count.txt

autonomous: true

# NOTE modèle/effort (GSD) : Task 1+2 = sonnet-4-6 / medium (impl UI + interface stub).
# Task 3 (batch réécriture des 78 fiches) = haiku-4-5 / low (volume mécanique, structure fidèle).
# Revue fable/opus avant intégration.
#
# DÉPENDANCE PARTAGÉE DÉCLARÉE (unique du fan-out) : ce plan CRÉE lib/installs.ts
# (interface install locale, contrat localStorage `skilltree.installs.v1` + event
# `skilltree:installs`). Le plan 03-04 (My Tree/TreeAudit) LIT ce MÊME contrat
# localStorage via son propre helper local — il n'IMPORTE PAS lib/installs.ts et ne
# modifie aucun fichier de ce plan. Couplage = contrat de clé localStorage, PAS de code
# partagé → les deux restent wave:1, depends_on:[], sans conflit de fichier. Phase 4
# (Supabase) remplace lib/installs.ts par /api/install sans réécrire l'UI Hub.

must_haves:
  truths:
    - "/hub rend, dans l'ordre des captures : GET STARTED (5 étapes, barre de progression), Modules (3 cartes), Fresh drops + FEATURED THIS WEEK, Most installed, Community pulse, Build logs — chaque section a un heading FR et gère son état vide"
    - "⌘K (ou Ctrl+K) ouvre la recherche ; taper un nom de skill réel (ex. « Billing » / « Facturation ») liste des résultats cliquables ; cliquer un résultat ouvre l'InstallModal (role=dialog) titré du skill avec bouton « Installer le skill → »"
    - "Cliquer « Installer le skill → » appelle install(slug) : le slug est écrit en state local (localStorage `skilltree.installs.v1`) et un second ⌘K sur le même skill montre l'état « Installé »"
    - "L'InstallModal affiche la fiche skill FR (titre, « Ce que ça fait », « Ce qu'il te faut ») lue depuis content/skills/<slug>.md — jamais la prose anglaise source"
    - "apps/web/content/skills/ contient 78 fiches .md FR réécrites (compteur 78/78) ; spot-check anti-verbatim sur 3 fiches : aucune phrase de ≥ 8 mots identique à captures/skill_files_full/<slug>.md"
    - "Preuves écrites dans orchestration/verify/p2/ : dom-assert.txt cité + hub.png + cmdk-modal.png + skill-files-count.txt (=78)"
  artifacts:
    - path: "apps/web/lib/installs.ts"
      provides: "interface install locale : useInstalls() (hook client), install(slug)/uninstall(slug)/isInstalled(slug), localStorage `skilltree.installs.v1` + event `skilltree:installs` (miroir exact du pattern lib/progress.ts)"
      min_lines: 40
    - path: "apps/web/components/hub/InstallModal.tsx"
      provides: "modale role=dialog aria-modal : fiche skill FR + bouton « Installer le skill → » qui appelle install(slug), Échap/backdrop ferme, focus géré"
      min_lines: 60
    - path: "apps/web/lib/skill-files.ts"
      provides: "loader des fiches FR : getSkillFile(slug) lit content/skills/<slug>.md (frontmatter + corps), fallback propre sur SKILL_FILES catalogue si md absent (build reste vert)"
      min_lines: 25
    - path: "apps/web/lib/hub-data.ts"
      provides: "sélections déterministes pour Fresh drops / Featured / Build logs (dérivées du catalogue + seed, aucun compteur d'install inventé non étiqueté ; Most installed dérive des installs locaux)"
      min_lines: 30
    - path: "tools/verify_p2.py"
      provides: "suite Playwright : sections Hub présentes, ⌘K→résultat→modale→install(slug) persisté, 78 fiches, screenshots"
      min_lines: 70
    - path: "orchestration/verify/p2/dom-assert.txt"
      provides: "preuve citée : headings des 6 sections, flux ⌘K→modale, install persisté, 78/78 fiches, spot-check anti-verbatim"
  key_links:
    - from: "apps/web/components/hub/InstallModal.tsx"
      to: "apps/web/lib/installs.ts"
      via: "install(slug) au clic sur « Installer le skill → »"
      pattern: "install\\("
    - from: "apps/web/components/ui/CommandBar.tsx"
      to: "apps/web/components/hub/InstallModal.tsx"
      via: "résultat ⌘K cliqué → ouvre l'InstallModal (au lieu de router.push /map?skill=)"
      pattern: "InstallModal"
    - from: "apps/web/components/hub/InstallModal.tsx"
      to: "apps/web/lib/skill-files.ts"
      via: "getSkillFile(slug) pour rendre la fiche FR"
      pattern: "getSkillFile"
---

<objective>
Livrer la page `/hub` fidèle à `captures/nav_hub.png` (GET STARTED 5 étapes, Modules, Fresh drops + Featured, Most installed, Community pulse, Build logs), la recherche ⌘K qui ouvre une **InstallModal** réelle sur un skill du catalogue, et l'interface stub locale `install(slug)` (localStorage) que la Phase 4 remplacera par `/api/install`. Inclut le batch de réécriture FR des **78 fiches skills** (`captures/skill_files_full/*.md` → `apps/web/content/skills/*.md`), jamais verbatim. Preuves sur rendu réel dans `orchestration/verify/p2/`.

Purpose: HUB-01 (sections), HUB-02 (⌘K + modale), HUB-03 (install local) + le lot 78 fiches (P2, docs/PLAN.md §graphe).
Output: page Hub complète, CommandBar branché sur InstallModal, `lib/installs.ts` + `lib/skill-files.ts` + `lib/hub-data.ts`, `content/skills/` (78 md), `tools/verify_p2.py`, preuves `orchestration/verify/p2/`.
</objective>

<execution_context>
@/home/nuveo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nuveo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/ARCHITECTURE.md            (§3.2 Hub, §4 flux install, D4/D9)
@docs/07_VISUAL_SPEC.md          (§Hub — structure exacte observée écran par écran)
@apps/web/lib/progress.ts        (PATTERN à cloner pour lib/installs.ts : localStorage + custom event + hook)
@apps/web/lib/catalog/index.ts   (SKILLS, SKILL_FILES, MODULES, skillBySlug, FRESH_DROPS/MOST_INSTALLED=[] vides)
@apps/web/lib/catalog/types.ts   (Skill, SkillFile {slug,title,what,needs,path})
@apps/web/components/ui/CommandBar.tsx   (⌘K existant — route vers /map?skill= ; À REBRANCHER vers InstallModal)
@apps/web/app/(app)/hub/page.tsx         (scaffold partiel actuel à compléter)
@apps/web/components/hub/CommunityPulse.tsx  (déjà OK — réutiliser tel quel)
@apps/web/components/hub/FirstWeekChecklist.tsx  (5 étapes plates — enrichir en GET STARTED avec progression)

Références visuelles (regarder AVANT de coder) :
- captures/nav_hub.png (structure complète), captures/dynamic/hub.png, captures/dynamic/hub.json (texte)
- captures/dynamic/hub_modal_carousel_designer.png / _cold_email_copywriter.png / _knowledge_base.png (la modale install cible + son JSON)
- captures/skill_files_full/*.md (78 fiches SOURCE — structure à reproduire, prose à RÉÉCRIRE en FR)
</context>

<constraints>
- **Chrome UI réécrit FR, jamais verbatim** : tous les libellés de sections/CTA sont des réécritures FR. Les 78 fiches sont RÉÉCRITES (structure fidèle : frontmatter name/description + sections « What It Does / Required API Keys / Configuration / How to Run… »), prose neuve — produit tiers payant. Spot-check anti-verbatim obligatoire dans les preuves.
- **Aucun chiffre inventé non étiqueté** (D3) : les compteurs d'install de l'original sont fictifs. En perso : `FRESH_DROPS`/`FEATURED` = sélection **déterministe** dérivée du catalogue (ex. slice ordonnée par secteur/slug), `MOST_INSTALLED` = dérivé des installs **locaux réels** (EmptyState quand aucun install). Ne jamais afficher un « N installs » fabriqué comme s'il était réel ; si une carte a besoin d'un âge/compteur de démo, l'étiqueter démo ou l'omettre.
- **Deux régimes de données jamais mélangés** : le Hub lit le catalogue statique (SKILLS/SKILL_FILES) ; l'état install vit en local (localStorage) via `lib/installs.ts`, jamais mélangé au catalogue.
- **Interface stub propre (Phase 4-ready)** : `lib/installs.ts` expose exactement `install(slug)`, `uninstall(slug)`, `isInstalled(slug)`, `useInstalls()` — signatures stables pour que P4 remplace le corps par des appels `/api/install` sans toucher l'UI. Miroir strict du pattern `lib/progress.ts` (même style d'event + hook).
- **A11y** : InstallModal = `role="dialog" aria-modal`, focus au 1er élément à l'ouverture, restore à la fermeture, Échap + backdrop ferment. ⌘K et Ctrl+K tous deux gérés.
- **Anti-faux-positif** : chaque assert de `dom-assert.txt` cite la commande/la mesure réelle. Jamais `|| true`. Serveur de preuve : `cd apps/web && pnpm build` (exit 0, NE PAS piper — SIGPIPE tronque) puis `npx next start -p 3010`. Playwright : `/home/nuveo/.higgsfield-login-venv/bin/python`. Réutiliser le style de `tools/verify_p1.py`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Sections Hub fidèles aux captures + hub-data déterministe</name>
  <files>apps/web/app/(app)/hub/page.tsx, apps/web/components/hub/FirstWeekChecklist.tsx, apps/web/components/hub/FreshDrops.tsx, apps/web/components/hub/FeaturedThisWeek.tsx, apps/web/components/hub/MostInstalled.tsx, apps/web/components/hub/BuildLogs.tsx, apps/web/lib/hub-data.ts</files>
  <action>
Regarder `captures/nav_hub.png` + `captures/dynamic/hub.json` avant de coder.

1. `lib/hub-data.ts` : exporter des sélections **déterministes** dérivées du catalogue (pas de Math.random, pas de compteur d'install fabriqué) :
   - `freshDrops()` : slice ordonnée stable de `SKILLS` (ex. 8 premiers par ordre secteur puis nom) rendue en cartes drop.
   - `featured()` : 1 job « FEATURED THIS WEEK » (pick déterministe, ex. premier job autonome de Marketing).
   - `buildLogs()` : 1-2 posts « Build logs » de démo, prose FR neuve, étiquetés démo (titre + âge relatif + extrait). Aucun verbatim des captures.
2. `FirstWeekChecklist.tsx` → **GET STARTED** : titre « Ta première semaine, en cinq étapes », progression `X/5` + barre, 5 étapes (numéro cerclé ou check), l'étape « installer ton premier skill » liste 3 chips skills colorés par secteur (Concepteur de carrousels · Rédacteur d'emails à froid · Base de connaissance), fermable ✕ (state local, pas de persistance requise ici). Réécriture FR.
3. Créer `FreshDrops.tsx` (bandeau « NEXT DROP … » + grande carte FEATURED + grille de cartes drop via `SkillCard variant="drop"`), `FeaturedThisWeek.tsx` (si tu la sépares), `MostInstalled.tsx` (top installés dérivés de `useInstalls()` — EmptyState quand vide), `BuildLogs.tsx` (posts de `hub-data`).
4. `hub/page.tsx` : composer l'ordre exact des captures — header (« N skills en ligne · … »), GET STARTED, Modules (existant), Fresh drops + Featured, Most installed, Community pulse (existant), Build logs, footer. Colonne droite (Most installed / Community pulse / Build logs) comme dans la capture si la largeur le permet, sinon empilé mobile.
  </action>
  <verify>`cd apps/web && pnpm build` (exit 0) puis `pnpm dev` : `/hub` montre les 6 sections dans l'ordre ; `MostInstalled` en EmptyState (aucun install) ; aucun « undefined ». `grep -n "Math.random" apps/web/lib/hub-data.ts` → 0.</verify>
  <done>Les 6 sections du Hub rendues, ordre + libellés FR fidèles à nav_hub.png, données déterministes sans chiffre inventé non étiqueté.</done>
</task>

<task type="auto">
  <name>Task 2: Interface install locale + InstallModal + ⌘K rebranché</name>
  <files>apps/web/lib/installs.ts, apps/web/lib/skill-files.ts, apps/web/components/hub/InstallModal.tsx, apps/web/components/ui/CommandBar.tsx</files>
  <action>
1. `lib/installs.ts` — **cloner le pattern de `lib/progress.ts`** : constante `STORAGE_KEY = "skilltree.installs.v1"`, `EVENT = "skilltree:installs"`, type `Record<string, true>`, `read()/write()` sûrs (try/catch, dispatch event). Exporter :
   - `useInstalls()` : hook client, écoute EVENT + `storage`, renvoie `{ installed: string[], isInstalled(slug), install(slug), uninstall(slug), count }`.
   - fonctions pures `install(slug)/uninstall(slug)/isInstalled(slug)` utilisables hors hook.
   Commentaire d'en-tête : « Phase 4 remplace le corps par POST /api/install ; signatures identiques. »
2. `lib/skill-files.ts` — `getSkillFile(slug)` : lit `content/skills/<slug>.md` (les 78 fiches créées en Task 3). Parser frontmatter (name/description) + corps markdown. **Fallback propre** : si le md n'existe pas encore, renvoyer les métadonnées catalogue `SKILL_FILES.find(slug)` (title/what/needs) pour que le build reste vert entre Task 2 et Task 3.
3. `InstallModal.tsx` — `role="dialog" aria-modal aria-label={fiche.title}`, backdrop + Échap ferment, focus au bouton principal à l'ouverture, restore `document.activeElement` à la fermeture. Contenu : eyebrow secteur, titre, « Ce que ça fait », « Ce qu'il te faut » (depuis `getSkillFile`), bouton **« Installer le skill → »** → `install(slug)` puis passe à l'état « Installé ✓ » (via `useInstalls().isInstalled`). Props : `{ slug: string | null; onClose: () => void }`.
4. `CommandBar.tsx` : garder l'ouverture ⌘K/Ctrl+K et le filtrage `SKILLS` ; **remplacer** `router.push('/map?skill=…')` par l'ouverture de l'`InstallModal` sur le slug cliqué (state `modalSlug`). Les résultats restent cliquables et labellisés. Monter `<InstallModal slug={modalSlug} onClose={…}/>`.
  </action>
  <verify>`pnpm build` exit 0 ; en dev : ⌘K → taper « Billing » (ou un nom réel) → résultat cliquable → InstallModal s'ouvre (role=dialog) → « Installer le skill → » → recliquer ⌘K sur le même skill affiche « Installé ». `grep -n "skilltree.installs.v1" apps/web/lib/installs.ts` présent.</verify>
  <done>install(slug) persiste en localStorage, InstallModal branchée sur ⌘K et sur la fiche FR, interface stub prête pour le swap P4.</done>
</task>

<task type="auto">
  <name>Task 3: Batch réécriture FR des 78 fiches skills + preuves Playwright</name>
  <files>apps/web/content/skills/ (78 fichiers .md), tools/verify_p2.py, orchestration/verify/p2/dom-assert.txt, orchestration/verify/p2/hub.png, orchestration/verify/p2/cmdk-modal.png, orchestration/verify/p2/skill-files-count.txt</files>
  <action>
**Batch fiches (haiku-4-5 / low — volume mécanique)** : pour chacun des 78 `captures/skill_files_full/<slug>.md`, écrire `apps/web/content/skills/<slug>.md` — **structure identique** (frontmatter `name`/`description`, puis sections « Ce que ça fait / Clés API requises / Configuration / Comment le lancer / … » calquées sur les headings source) mais **prose FR entièrement réécrite**, jamais une phrase copiée telle quelle. Peut se faire en sous-lots (ex. 4×20) pour ne pas saturer le contexte. Les slugs doivent matcher exactement `SKILL_FILES[].slug` (sinon le loader Task 2 tombe en fallback).

**Preuves** — écrire `tools/verify_p2.py` (Playwright sync, `/home/nuveo/.higgsfield-login-venv/bin/python`, serveur :3010 up), style `tools/verify_p1.py`, chaque check imprime PASS/FAIL + valeur mesurée, exit 1 au 1er FAIL :
1. `/hub` : les 6 headings présents (« Ta première semaine », « Modules », « Fresh drops »/« FEATURED THIS WEEK », « Most installed »/« plus installés », « Community pulse »/« communauté », « Build logs »). Screenshot `hub.png`.
2. ⌘K : `page.keyboard.press("Meta+k")` (fallback `Control+k`) → dialog recherche visible → taper un nom réel (dériver le 1er `SKILL_FILES[].title` ou un mot du nom) → au moins 1 résultat → cliquer → `role=dialog` de l'InstallModal visible avec « Installer le skill → ». Screenshot `cmdk-modal.png`.
3. install persiste : cliquer « Installer le skill → » → assert `localStorage['skilltree.installs.v1']` contient le slug (`page.evaluate`) → fermer, rouvrir ⌘K sur le même skill → état « Installé » présent.
4. 78 fiches : compter `content/skills/*.md` = 78, écrire le compte dans `skill-files-count.txt`. Anti-verbatim : pour 3 slugs, comparer hors-browser le md FR au source `captures/skill_files_full/<slug>.md` — asserter qu'aucune fenêtre de 8 mots consécutifs n'est identique ; consigner les 3 verdicts dans `dom-assert.txt`.

Écrire `dom-assert.txt` : date, commandes serveur, chaque check + mesure citée + PASS/FAIL, + verdict fidélité `hub.png` vs `captures/nav_hub.png`. Tuer le serveur à la fin. Si FAIL → corriger le composant, relancer ; ne jamais éditer le txt à la main pour verdir.
  </action>
  <verify>`ls apps/web/content/skills/*.md | wc -l` = 78 ; `/home/nuveo/.higgsfield-login-venv/bin/python tools/verify_p2.py` (serveur :3010) → tous PASS, exit 0 ; `hub.png` et `cmdk-modal.png` > 30 Ko ; `skill-files-count.txt` contient « 78 » ; dom-assert.txt contient les 3 verdicts anti-verbatim.</verify>
  <done>78/78 fiches FR réécrites et rendues dans la modale, flux ⌘K→install prouvé sur rendu réel, preuves reproductibles dans orchestration/verify/p2/.</done>
</task>

</tasks>

<verification>
- `pnpm build` vert (prebuild assert-graph inclus).
- `tools/verify_p2.py` exit 0 : 6 sections Hub, ⌘K→résultat→InstallModal, install(slug) persisté localStorage, 78/78 fiches, spot-check anti-verbatim (3 verdicts).
- `orchestration/verify/p2/` : dom-assert.txt + hub.png + cmdk-modal.png + skill-files-count.txt(=78).
- Verdict fidélité `hub.png` vs `captures/nav_hub.png` consigné.
</verification>

<success_criteria>
1. Screenshot `/hub` vs `captures/nav_hub.png` : GET STARTED 5 étapes, Modules, Fresh drops+Featured, Most installed, Community pulse, Build logs (Criterion 1 de phase).
2. ⌘K sur un nom de skill réel → résultat cliquable → InstallModal « Installer le skill → » (assert DOM) ; install(slug) en state local persisté.
3. 78 fiches skills FR réécrites (compteur 78/78) + spot-check anti-verbatim. Preuves dans `orchestration/verify/p2/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-vues-modules-ui/03-01-SUMMARY.md`
</output>
